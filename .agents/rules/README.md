# Scoped agent rules

`AGENTS.md` is the canonical repository-level rule set. Files here may add narrowly scoped rules for a directory or task family, but may not weaken security, tenant isolation, PR discipline, issue scope, release gates or delivery-state synchronization.

Prefer a skill or workflow when the content describes a procedure rather than a persistent rule.
