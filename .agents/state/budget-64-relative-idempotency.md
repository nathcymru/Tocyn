# #64 relative idempotency encoding — partial delivery

13 September 2026. Issue #64 covers storage, bounded reservations and recovery; this slice also advances local #140 acceptance. Root approved the lossless contract after preserved-candidate accounting inspection. Progress and baseline/forecast fields remain unchanged. No full budget or Beta.2 completion claim.

Format 4 encodes an idempotency key as a one-string suffix tuple only when it begins with the exact nonempty holder ID. Decoding reconstructs the original key; literal keys remain literal. Strict relative tuple, identifier and version checks fail closed. Raw legacy and formats 2/3 remain readable. A format-4-written store requires a format-4-capable reader: no downgrade compatibility is claimed. No record, original identity, charge, allocation, window, attempt, certificate or retention policy is pruned or forgiven. Standalone encodedGrantBytes and recoveryHeadroom remain unchanged.

Preserved COPY evidence: all 195 grant records and every semantic state field deep-equal after roundtrip; every standalone grant-byte bound remains unchanged. Format 3 encoding of 97,428 bytes becomes 89,364 bytes (8,064 saved; 192 exact prefixes). The original/live store was not mutated for this comparison.

Controlled cold-allocation sequence on copied state, including the existing two-retirement limit separately for refresh and reserve: the fifth requested grant is resource-granted but format 3 encoded 98,618 + headroom 24,414 = 123,032 exceeds 122,880. The same semantic input under format 4 is 90,344 + 24,414 = 114,758. This demonstrates additional bounded metadata room. It does not prove every observed browser refusal had that cause or promise perpetual capacity. Later isolated copied-store due reads succeeded at a later clock, so transient original timer failures remain attributed conservatively.

Checks: codec units 16/16; full server ordinary units 964/964 across 94 files; server typecheck and scoped ESLint pass. Native metadata/reload proof passes 1/1 (1.19s): 292 synthetic full-charge uncertain tombstones, format 3 bytes 112,846 versus format 4 bytes 100,582; the same three concurrent cold reservations all reject under format 3 and all succeed under format 4. Native reload preserves all original grants/charges, followed by idempotent replay and delivery-attempt exhaustion. Native fixture/config typecheck passes. Miniflare emitted RPC-stub disposal warnings; the owned fixture was disposed. Initial fixture-only corrections fixed generated wrapper syntax and expected safe rejection rather than a thrown error. No ordinary CI native job added. No Copilot requested.

The preserved 56640 preview remains on merged #309 code with both due tenants paused after admission rejection. No automatic resume, reset, top-up, early expiry or liability forgiveness is authorized by this change. Root owns final source review, exact-head gates and any future code-only preview upgrade/resume.
