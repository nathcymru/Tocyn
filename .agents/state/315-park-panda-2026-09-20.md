# #315 Park/Panda corrective checkpoint — 2026-09-20

- Integration branch and PR: `codex/315-park-panda-corrective`, PR #316. The unrelated #243 checkout and branch remain untouched.
- Coordinator verified a source audit of all active app routes and shared UI. No active native button/input/textarea/select, pseudo-Park marker, or legacy icon/editor import was found; the Park guard now parses JSX so self-closing native controls cannot escape the check.
- GPT settings lane: retained cached General Settings and Automation data on failed refresh, added Park retry alerts and regression tests; converted Usage credentials presentation to installed Park Card/Alert. Coordinator reviewed the diff.
- GPT portal lane: replaced initial protected-route, lazy-route, ticket-list and ticket-detail loading text/empty states with Park skeletons and screen-reader status; added deferred-read regressions. Coordinator reviewed the diff.
- Coordinator: fixed widget rendering for persisted string feature switches (`"true"`/`"false"`), including a no-enabled-feature case, with a regression test.
- Ollama Cloud `gpt-oss:120b` made one bounded proposal for the Park guard; the coordinator implemented and verified an AST-based version. Ollama Local was not invoked: 4.39 GiB available against a 6 GiB cold-model guard (1.61 GiB shortfall). No memory guard was weakened and no unrelated process was stopped.
- Verified locally: Panda codegen/cssgen; dashboard 466/466, portal 77/77, widget 16/16, UI 35/35 tests; Park and icon guards; dashboard, portal, widget builds; widget and UI typechecks; portal lint; `git diff --check`.
- `npm run lint --workspace=apps/dashboard` was attempted but that workspace has no lint script. The required CI lint command is portal lint plus server ESLint; this is not recorded as a passing dashboard lint check.
- Still required before #315/Beta.2 acceptance: fresh exact-head CI/CodeQL after push, authenticated Safari visual checks at light/dark and 200% zoom, keyboard and reduced-motion checks, VoiceOver and broader #140 release gates. Authenticated browser work is waiting for the Mac to be unlocked; source/test success alone is not visual acceptance.

## Follow-on verified batch

- PR head `c4bdd2af` subsequently passed exact-head lint, typecheck, build, test and CodeQL. A route/CSS audit then found two bare unresolved Panda values in generated CSS: `text.default` (11 active uses) and `bg.muted` (one active use). The coordinator replaced them with existing `text.primary` and `bg.subtle` tokens, regenerated CSS, and expanded the guard to reject bare semantic values.
- GPT settings lane: Support States now retains loaded definitions on failed refresh/page load with Park Alert and explicit retry. Login and MFA errors now use Park Alert while retaining described-by IDs and retry focus. The unused legacy auth alert style was removed.
- GPT shared UI lane: confirmation errors now render installed Park Alert inside the existing Park Dialog composition, with busy/focus contracts intact.
- Verified after these edits: dashboard 467/467; focused auth/support-state 13/13; UI 36/36; UI typecheck; dashboard build; Park/icon guards; `git diff --check`. Portal/widget code did not change in this batch.
- Headless local login evidence at normal size, 200% zoom, and dark theme is under `/tmp/tocyn-beta2-browser/` (not committed). Atkinson rendered and no login-route browser errors were observed. Authenticated Inbox evidence remains blocked by unavailable synthetic credentials in that headless session; physical Safari and VoiceOver still need an unlocked Mac. API `/health` returned one transient 503 during concurrent work, then two consecutive 200 responses; local fixture stability remains to be confirmed during acceptance.
- Further bounded presentation gaps from read-only audit: KnowledgeBrowser preview loading/error, portal magic-link verification loading and ticket-SLA loading, portal ticket-create error, widget submit status. These are not called complete by the token/alert batch.

## Cross-app recovery checkpoint

- Exact-head required CI and CodeQL subsequently passed at `ce6946a9` (8/8 checks). This receipt refers to the next local batch before its own push and exact-head CI.
- GPT dashboard lane: KnowledgeBrowser preview loading uses Park Skeleton/status; preview failure uses Park Alert with Retry. Operator theme restore timeout keeps authenticated children mounted under fallback tokens and exposes inline Park Alert/Retry, so form DOM identity and content survive restoration. Focused dashboard tests 18/18 and dashboard typecheck passed.
- GPT portal lane: magic-link verification uses labelled Park Progress while pending; ticket SLA loading uses Park Skeleton with announced status. Focused portal tests 11/11 and portal typecheck passed.
- Coordinator: ticket-create errors and closed-ticket notice use Park Alert; widget submit progress uses Park Progress; shared Progress applies its accessible label to the actual Ark progressbar Track. Removed 15 unused portal style keys and 8 no-op `css({})` keys after checking active references. Portal full suite 80/80, widget full suite 16/16, UI full suite 36/36; focused Verify 5/5 and widget TicketForm 2/2 passed after the shared Progress label correction.
- Dashboard, portal and widget builds passed; UI and widget typechecks, portal lint, Park UI source guard, icon guard and `git diff --check` passed. Panda-generated CSS updated with the affected source. The portal cleanup initially exposed a remaining `notice` consumer; it was converted to Park Alert before the passing portal suite/build. A first Progress label attempt attached to Ark Root rather than Track and failed; the Track correction passed the focused tests.
- One Ollama Local Granite 4.2 3B bounded proposal returned after its memory guard allowed inference. Its suggestion to place `aria-describedby` on the error itself was rejected; fields retain the correct relationship to the Park Alert. Ollama Cloud had one earlier bounded proposal; GPT reviewed and applied code. No worker proposal is counted as verification.
- Read-only shared-layer audit confirmed installed Park/Ark source and Panda recipes for active controls, with no substantive active visible-control bypass in `packages/ui/src/park.tsx` or installed `components/ui`. The wrapper compositions are for Park parts and Tocyn-specific layout.
- Local dashboard and API returned HTTP 200 after this batch. Authenticated visual, 200% Inbox/detail, reduced-motion and VoiceOver evidence remain open. The Mac is locked and synthetic credentials are not available through a safe automated channel; no secret material was read or exported. Beta.2 ETA remains unbounded until these and #140 gates are evidenced.

## Follow-on Park presentation checkpoint

- PR head `5655b1c7` passed all eight required exact-head CI and CodeQL checks. Issue #315 received a partial progress receipt at that revision; the PR remains draft.
- Read-only dashboard/portal/widget/recipe audits found no active native control fallback but identified plain recovery states, a square navbar Avatar override and an inset search-focus override. The next batch replaces Inbox quick-view/list/workspace, ticket-detail refresh/draft/support/composer, dashboard stale metrics, shell activity, portal route-load/local capture and widget AI-waiting states with installed Park Alert, EmptyState, Progress or Skeleton parts. The local capture page no longer reports zero data during a failed read/reset. The official Avatar shape applies; the Park Input focus-visible ring is no longer overridden by the search recipe.
- Removed superseded TicketDetail and shell alert/notice/draft-status Panda slots; regenerated Panda code and CSS. The portal route-load EmptyState now accepts a heading ref so existing error-heading focus and real document reload remain intact.
- Verification for this unpushed batch: dashboard 470/470, portal 82/82, widget 16/16 and shared UI 37/37 tests; Park source/CSS guard checked 101 active application files; icon/editor regression guard 6/6; dashboard, portal, widget and shared UI typechecks; dashboard, portal and widget production builds; portal lint; `git diff --check`. Initial TicketDetail focused tests found that notice text itself needed `role=status`; the Park Description now carries it, and rerun focused tests passed 56/56. No current check failure is claimed as passing without the correction.
- Source search still finds seven plain `role=alert` branches in Knowledge, NewTicketDialog, API Key, Groups and Support States; these remain a bounded follow-up before claiming all legacy presentation is removed. Actual navbar/avatar/search geometry, light/dark and 200% reflow still need authenticated browser evidence. Exact-head CI/CodeQL for this new batch remains pending until push.

## Raw-alert and retired-workspace cleanup checkpoint

- PR head `bb030f15` passed all eight exact-head CI and CodeQL checks. The local follow-on batch converts the remaining raw `role=alert` branches in active dashboard pages and components (Groups, Knowledge deletion, New Ticket, API Key, Support States, draft navigation, authenticated image preview, Tiptap link validation, route load, global inbox alert, ticket SLA/actions/assignment) to installed Park Alert anatomy. Existing retry, idempotency, draft, focus, authorization and described-by contracts were retained; focused tests cover the relevant actions.
- The ticket SLA initial detail load now uses two Park Skeleton lines with an announced busy status. The unused `WorkspaceShell`/`WorkspaceRegion` family and unused `TocynIconProps` alias were removed from the shared public API, source, tests and type fixtures. The visual browser fixture now uses a semantic `main` and records the active Park source hash. The retained-behavior matrix notes this removal.
- The Park source guard now rejects raw paragraph/div/section alert presentation in reachable active app JSX, retired workspace source/API names, and earlier control fallbacks. A post-change search found no raw active app `role=alert` branch. Panda codegen/cssgen passed (336 extracted files). Coordinator corrected an assignment-alert composition so retry/refresh buttons remain rendered even if the action message is empty; focused assignment and SLA tests passed 16/16 after that correction.
- Local verification for this unpushed batch: dashboard full suite 474/474; UI 36/36 and UI typecheck; dashboard typecheck/build, portal build, widget build; Park guard (99 active files); icon/editor guard 6/6; `git diff --check`. A headless shared-control browser fixture passed form serialization, dialog/focus, tabs, listbox, combobox, splitter keyboard bounds, popover dismissal, target sizing and reduced motion under default and radical CSS; no-CSS behavior passed without presentation assertions. The fixture ran against the dirty worktree at base `bb030f15`, so it is not exact-head authenticated app evidence.
- Still open: exact-head CI for this batch after push, authenticated light/dark/200% browser review, keyboard/VoiceOver, widget CSS decision, signing and #140 release gates. The Mac remained locked when checked through the approved computer-use tool, and no synthetic credentials were extracted.

## Cached-data recovery checkpoint

- `d781ff42` was pushed to PR #316 and all eight required exact-head CI/CodeQL checks passed. The headless shared-control fixture passed from a clean exact head under default/radical/no CSS; it does not replace authenticated app evidence.
- This follow-on batch preserves cached content on failed background refresh in Ticket Fields, SLA policy, Users, Widget Channel and Email Channel. Each has a Park Alert with Retry while initial missing-data failure remains a retryable EmptyState. SLA unsaved edits survive successful retry; if the policy revision changed, Save is fenced until the operator explicitly chooses to apply the retained edits to the new revision. Widget and email setting saves remain blocked during a failed refresh. Portal reply-refresh warning and ticket-create success use Park Alert. Ten ticket-detail secondary/retry buttons now use the official plain variant rather than conflicting custom foreground overrides.
- Coordinator reviewed all returned GPT edits and corrected a stale email-settings save regression before integration. A concurrent build initially failed because the Email Channel worker had not finished moving a derived flag after its query declaration; the corrected source passed the subsequent dashboard typecheck and build.
- Local verification for this unpushed batch: dashboard 483/483, portal 82/82, widget 16/16, UI 36/36; dashboard, portal, widget builds; dashboard and UI typechecks; Park guard (99 active files); icon/editor guard 6/6; `git diff --check`. Panda codegen/cssgen regenerated `packages/ui/src/styles/panda.css` for newly extracted classes. Focused SLA/Ticket Fields/Ticket Detail 63/63 and portal conversation 22/22 also passed.
- Next: commit/push this coherent batch, check exact-head CI/CodeQL, update PR/issue evidence. Authenticated route geometry, populated/empty browser states, 200% zoom, keyboard/reduced-motion and VoiceOver remain open while the Mac is locked; no production deployment or Beta.2 acceptance is claimed.

## Park Link composition and cross-app review checkpoint

- `8968c360` passed all eight required exact-head CI and CodeQL checks. This follow-on batch converts active dashboard shell/navigation, search results, Inbox table, Ticket Detail, action links, Usage help and sanitized Markdown links to installed Park Link composition. It also converts portal brand, ticket-list/detail and local-only captured-login links; the widget success view now uses Park Alert anatomy and a semantic Indicator icon. Existing destinations, focus, keyboard, draft/navigation and ShadowRoot behavior were retained. The local capture link permits only approved loopback `/verify` origins without embedded URL credentials.
- A TypeScript-AST regression guard now rejects uncomposed active native anchors and Router links. Its narrow Inbox parent-focused row exception requires `tabIndex={-1}`. Self-tests cover direct composition, nested/multiple children, self-closing tags and Router aliases. Independent read-only GPT review found and corrected three initial guard edge cases before integration.
- Coordinator reviewed the application/source diffs and inspected the generated CSS; independent dashboard and portal/widget reviews found no remaining blocking link regression. The shared Alert Indicator preserves its default InfoIcon while allowing a semantic child in the same official slot, with a focused shared-UI test.
- Local verification before the final two bounded review corrections: dashboard 490/490, portal 87/87, widget 16/16, shared UI 36/36; dashboard, portal, widget and shared UI typechecks; Park guard across 99 active files; icon/editor guard 6/6; dashboard, portal and widget production builds. After the review corrections, focused shared UI/widget/portal tests passed 15/15 and their typechecks passed; Panda regeneration and affected widget/portal builds passed. No claim of exact-head CI is made for this unpushed batch.
- A read-only follow-up audit found field-anatomy gaps in Support States, Email Channel and Operator Theme settings, plus a custom RichComposer autocomplete needing separate keyboard/focus review. A separate Tiptap audit found potential content loss for declared task-list and inline-image Markdown, and unsupported tables; read-only editor toolbar buttons also remain enabled. These require focused failing round-trip tests and a preservation policy before implementation. They remain queued after this coherent link checkpoint. Authenticated light/dark/200% Inbox geometry and VoiceOver remain blocked by the locked Mac and unavailable signed-in review session. The local dashboard and API health endpoints both returned HTTP 200; this is not Beta.2 acceptance.

## CI correction and remaining-route checkpoint

- The cached-data recovery batch was pushed as `0d7cf365`. Its exact-head lint, typecheck, build and CodeQL jobs passed, but required `Tocyn / test` failed 482/483 on a timing-sensitive TicketDetailWorkflow assertion. The test navigated before the workspace selection preference had reported a successful save. The corrected test waits for the visible saved state, then checks navigation and the PUT; focused TicketDetailWorkflow passed 50/50. This failure remains an open exact-head gate until the next push/CI pass.
- GPT dashboard lane corrected Groups dialog stale member/agent list presentation and Support States cached-empty refresh: Park Alert/Retry with retained rows or first-state draft; initial missing data still uses EmptyState. Stale membership mutations remain disabled. Focused Groups 12/12 and Support States 7/7 passed, with dashboard typecheck.
- GPT portal lane migrated customer verification from a single input to installed Park Pin Input six-cell anatomy, preserving paste, autofill, focus, error and challenge behavior. Focused portal tests 19/19 passed; actual portal workspace typecheck passed. GPT Panda lane fixed compact sidebar icon/link geometry and global-search scope width/reflow in Tocyn layout recipes without overriding the installed Park Select; focused layout/search tests 43/43 and Panda generation passed. True browser geometry remains unverified.
- Coordinator validated provider usage response shape before rendering metrics; malformed required readings show a retryable error rather than false zero, while absent optional Durable Objects/Vectorize readings show Unavailable and omit progress. Focused Usage tests 7/7 and dashboard typecheck passed. Existing legitimate zero readings remain zero.
- Current unpushed local verification: dashboard 489/489, portal 83/83, widget 16/16, UI 36/36; dashboard, portal and widget builds; shared UI typecheck; portal lint; Park/icon guards; `git diff --check`. Panda CSS regenerated. Next: push this checkpoint and obtain all eight exact-head gates, then update PR/issue. Authenticated light/dark/200%/VoiceOver and #140 gates remain open.

## Tiptap preservation and Park Field checkpoint

- The last pushed PR #316 head `e5f820441c641f94834a99e237a0ba47a864022f` passed all eight required exact-head lint/typecheck/build/test/CodeQL checks. This entry describes the next local batch before its own push.
- Tiptap now uses one shared Markdown extension set in the reply composer and knowledge editor. Task lists, supported marks/nodes, image references and unsupported GFM tables have round-trip coverage. Images render a non-fetching text placeholder; only explicit credential-free HTTP(S) links become clickable. Unsafe/relative Markdown links remain stored but inert. The controlled knowledge editor preserves the caret on value echo; autocomplete has one Tiptap write; read-only formatting actions are disabled. The Markdown contract documentation was updated.
- GPT route lanes converted Support States, Email Channel, Operator Theme preferences, SLA Settings, Knowledge Browser, New Ticket, Ticket Fields, Ticket Assignment, Groups and Filters settings fields to installed Park Field/Select label anatomy, with focused test assertions. The coordinator reviewed the diffs and corrected the SafeMarkdown preview to match the editor's link policy. The initial full dashboard run saw the new link assertion before the module edit had settled (510/511); its focused rerun passed 15/15 and the final full run passed 511/511.
- Panda codegen/cssgen, shared UI 37/37 tests, dashboard typecheck and production build, Park source/CSS guard (100 active files), Phosphor/icon guard 6/6, and dashboard 511/511 passed. Dashboard `/login` and local API `/health` each returned HTTP 200. `git diff --check` was clean before final state entry. No authenticated or VoiceOver visual acceptance is inferred.
- One bounded Ollama Cloud `gpt-oss:120b` Tiptap proposal was rejected after review because it contradicted measured task-list loss and suggested an unsupported catch-all. No retry was made. Ollama Local was not forced for this coupled editor fix. Independent GPT review found no further high-confidence editor regression; real browser image network/focus/IME behavior remains unverified.
- Read-only next-batch audit identified raw Field/Select labels in `AutomationPage`, `KnowledgeEditorPage`, `OperatorCapacityPanel`, `TicketDetailPage`, and the composer link dialog. These remain open, as do authenticated light/dark/200% Inbox geometry, keyboard/reduced-motion, VoiceOver, exact-head CI for this batch, and #140 release gates. The Mac remains locked; no fixture credentials were accessed.

## Route label and status integration checkpoint

- `144e49bb362370af8abd5e974a3749d28d89bcf1` passed all eight required exact-head lint/typecheck/build/test/CodeQL checks. PR #316 and issue #315 received evidence-based progress receipts; both remain open/draft. This entry describes the next local batch before its own push.
- Three bounded GPT lanes migrated Automation, Knowledge Editor, Ticket Detail context/custom fields, API Key and Agent Permissions statuses, portal magic-link success and reply statuses, and appearance/preferences feedback to installed Park Field, Select, Alert or Progress anatomy. The coordinator integrated the capacity-limit Field and error Alert, Tiptap link-dialog Field, an explicit label relationship for the knowledge contenteditable, and removal of the obsolete Account Settings raw-label selector. Values, draft saves, retry, CAS/identity fencing, focus restoration and tenant behavior were retained. The portal widget was not changed.
- The Park regression guard now rejects lowercase raw `<label>` in reachable active dashboard/portal/widget TSX, with AST self-tests; official Park slot labels remain allowed. A source search found no active raw button/input/select/textarea/label elements, Tailwind-only classes, or legacy icon/editor imports. The icon regression guard still excludes historical documentation and transitive lock metadata by policy.
- Local verification: dashboard 513/513, portal 87/87, widget 16/16 and shared UI 37/37 tests; dashboard, portal and widget production builds; dashboard and portal typechecks through builds; widget font-bundle verification; Panda codegen/cssgen; Park source/CSS guard across 100 active application files with 8/8 self-tests; Phosphor/editor guard 6/6; `git diff --check`. The new dynamic Ticket Detail Field/Select mutations and focus tests passed 50/50; portal focused status tests passed 35/35; appearance/preferences focused tests passed 44/44.
- Physical Safari remains locked on a fresh inspection; authenticated light/dark/200% Inbox geometry, populated/empty states, keyboard/reduced-motion and VoiceOver evidence remain open. No credential material was read or exported, and no remote deployment or Beta.2 acceptance is claimed. Exact-head CI/CodeQL for this new batch remains pending until push.

## Inbox row and persona alignment checkpoint

- PR head `3023f1c31a64adf33fc0561def7385951f09d018` passed all eight required exact-head CI/CodeQL checks. PR #316 and issue #315 received updated evidence receipts. Headless unauthenticated login images in light/dark and an emulated narrow 200% viewport found no horizontal overflow or browser errors, but do not establish authenticated or native-zoom acceptance.
- GPT lanes corrected the shell account Avatar/status relationship and rebuilt Inbox ticket rows as spaced surfaces with a left SLA anchor, reference-before-subject hierarchy, stable marker slots and a hover/focus/Space-expandable preview. The coordinator replaced an imperative Account Settings security navigation button with an installed Park Link composed with React Router. Existing account menu focus/actions, Inbox roving focus, swipe, drafts, routing and pagination remain.
- Coordinator source review and Panda codegen/cssgen passed. Focused Layout/Account/Inbox tests passed 63/63; the full dashboard suite passed 514/514. Dashboard typecheck and production build passed. Park source/CSS guard passed across 100 active app files with 8/8 AST self-tests; Phosphor/editor guard passed 6/6; `git diff --check` passed.
- Read-only settings audit found installed Park Menu anatomy already active in SettingsLayout. Its Settings/Channels groups are flat rather than cascading. An Inbox data-contract audit found that the supplied reference's organisation/tier filters, full quick-view persistence, exact ranking and filtered-scope statistics need explicit tenant-scoped server contracts; the current page has exact email, owner, date, search and existing sorts, with page-local suggestions/statistics. Product questions on organisation/tier and quick-view ownership are pending while independent presentation work continues.
- Next: commit/push this coherent checkpoint and obtain exact-head CI. Then address Inbox header/filter composition and shell geometry without inventing ranking/statistics or weakening tenant/budget/authorization behavior. Authenticated Safari review remains blocked by the locked Mac; Beta.2 and #140 gates remain open.

## Shell, Inbox metrics, theme and editor acceptance checkpoint

- The preceding Inbox-row/persona batch was pushed as `e9c39b8c0a1d011b053db4ee1919afae4e7df1c8`; all eight required exact-head CI/CodeQL checks passed. The next local batch is on the same #315 branch and is not yet exact-head validated.
- GPT lanes and coordinator updated the shell header/body hierarchy and single account menu, compacted the 320 CSS-pixel header from 251px to 155px, added an honestly page-scoped Inbox metric band with unavailable SLA state, preserved filters/retry/focus, restored the optional ShadowRoot-only `LUMINA_WIDGET_CSS` hook, fixed the disconnected Phosphor glyph, and migrated the first simple reference dialog to direct installed Park slots. The active Tailwind-class guard now follows static same-file aliases. `fg.subtle` uses a 4.5:1-readable Park semantic alias; Park code/table text uses Inter/tabular features. The validated tenant theme threshold is 4.5:1, with existing fallback behavior for old stored palettes below it. A Tiptap knowledge textbox announces read-only state; actual ProseMirror paste/drop image acceptance and rejection are tested.
- Local checks after integration: dashboard 525/525; portal 87/87; widget 17/17; shared UI 41/41; focused server settings 15/15; dashboard, portal and widget builds; server/widget/UI typechecks and dashboard/portal typechecks through their builds; portal/server lint; Park guard 12/12 self-tests across 100 active files; Phosphor/editor guard 6/6; Panda codegen/cssgen; `git diff --check`. Two attempted standalone `typecheck` workspace scripts for dashboard/portal do not exist; their production builds run TypeScript and passed. Earlier focused Tiptap 58/58 and reference-dialog 4/4 checks also passed.
- Synthetic, write-blocked Chromium inspected the dirty worktree at 1280 and 320 CSS pixels: no document overflow or search-icon overlap; one account trigger, bounded menu and Escape focus return; eight synthetic Inbox rows. At 320px the corrected header measured 155px; dark/light and 2x DPR-emulated screenshots are under `/tmp/tocyn-shell-visual-check.json`. This is not fixture authentication, native 200% zoom or tenant evidence. A six-route synthetic audit under `/tmp/tocyn-route-visual-audit.json` found mobile clipping of the Knowledge and Groups empty states. The Knowledge cause is an unconditional two-column compound recipe; both are next-batch defects, not accepted exceptions.
- Read-only audit found the Settings route picker already uses installed Park Menu anatomy, but Settings and Channels are flat groups rather than a nested Channels flyout. Sixteen active `TocynDialog`/`TocynConfirmDialog` usages were identified; these wrappers render installed Park parts, but their compatibility API still needs staged removal. The first reference-dialog caller is now directly composed. Real organisation/tier filtering and private versus tenant-shared quick views remain product decisions; no backend contract was invented.
- Ollama Local remained memory-blocked at the previously observed 0.45 GiB guard shortfall, with no process termination or guard reduction. A separate free Cloud `gpt-oss:120b` checklist proposal used 617 input and 500 output tokens but was truncated and misstated viewport dimensions and guard coverage; the coordinator did not use it as evidence. Hosted Graphify previously returned an OAuth invalid-grant error; verified local source and deterministic checks were used instead.
- Next: push this reviewed checkpoint, require eight exact-head CI/CodeQL checks, update PR #316 and issue #315, then fix Knowledge/Groups narrow empty states and continue route/browser acceptance. Authenticated Safari, native 200% zoom, reduced-motion, VoiceOver, maintainers' visual approval, signing and #140 gates remain open. No remote deployment or Beta.2 acceptance is claimed.

The batch above was pushed as `d95d3f415a0d0c760ff8ac1b457e5815664c92d9`; all eight required exact-head checks passed. PR #316's description and issue #315 received the evidence receipt. The Project item remains In progress; no arbitrary progress percentage or baseline date was changed. The next uncommitted work fixes Knowledge/Groups mobile empty states and continues direct installed Park Dialog migration.

## Next corrective batch prepared for PR #316

- All active `TocynDialog`/`TocynConfirmDialog` callers in dashboard routes, shell, settings and the browser fixture were replaced by installed Park Dialog anatomy. The shared compatibility file and package export were removed. The Park guard now rejects their return and checks the browser fixture, including raw labels. Busy, retry, focus and mutation tests remain attached to the direct callers.
- The Settings Channels menu now uses a nested Park Menu and stays within a 320px viewport: submenu x8–248 in normal and DPR2 synthetic browser checks, with ArrowRight/ArrowLeft/Escape and authorised navigation exercised. Mobile Navigation is a 224px left drawer with a 40px right-aligned Close control, no document overflow, and focus return after Close/Escape. Logout confirmation Escape returns focus to Account options; busy Escape remains blocked.
- Knowledge and Groups zero states moved outside data tables to keep their Park EmptyState content readable at 320px. The Appearance/Preferences global button/label overrides were removed; local Panda layout keeps Selects at 40px and confines narrow preferences controls to the 206px section. The widget rebinds its Park palette inside the ShadowRoot and uses light-scoped semantic backgrounds; in a synthetic dark portal host its header text contrast rose from 1.15:1 to 16.39:1 at 320/1280, with no clipping or external requests. Widget remains intentionally light-only pending any separate approved mode contract.
- Coordinator checks on this uncommitted batch: dashboard 537/537, shared UI 40/40, portal 87/87, widget 18/18; shared/UI/dashboard typechecks, dashboard/portal/widget builds, Park guard 12/12 across 100 active files, icon guard 6/6, CSS/codegen, browser fixture, widget dark-portal browser regression 4/4, and `git diff --check` passed. Synthetic browser receipts and screenshots are under `/tmp/tocyn-settings-menu-audit.json`, `/tmp/tocyn-layout-dialog-audit-after.json`, `/tmp/tocyn-mobile-drawer-close-320.json`, `/tmp/tocyn-appearance-park-2026-09-20/`, and `/tmp/tocyn-widget-dark-portal.json`. These are not authenticated Safari, native 200% zoom or VoiceOver acceptance.
- Ollama Local memory guard recovered from 2.55 to 4.3 GiB available and accepted one bounded Granite proposal request. The proposal conflated mobile Navigation and Logout steps, so it was rejected as acceptance evidence. Cloud's earlier truncated proposal remains rejected. No paid fallback, process termination or guard relaxation occurred.
- Next: commit/push this coherent batch, require eight exact-head CI/CodeQL checks, update PR/issue evidence, then restart the disposable fixture with fresh synthetic credentials and run authenticated browser/Safari accessibility acceptance. Product decisions on organisation/tier filter meaning and quick-view sharing remain pending; do not invent backend semantics. No Beta.2 acceptance or remote deployment is claimed.

## Responsive review fixture checkpoint

- The prior corrective batch was pushed as `bfe55d953df0aa02df6b75a275d35e08bb89ef50`; all eight exact-head CI/CodeQL checks passed. PR #316 and issue #315 received progress receipts. The PR remains draft.
- Filters and API Keys render loading, empty, and failed states outside their wide Park tables; populated rows retain horizontal table scrolling. Knowledge editor headings wrap at 320px and stay on one line at desktop width. The Inbox now puts rows directly after its toolbar by default; its page-scoped metrics live in the optional Quick statistics drawer, and New Ticket remains a labelled 40px Park button in the toolbar. Existing customer/search filter controls remain functional, but the open mobile filter panel is still taller than the supplied reference.
- Coordinator checks after integrating these changes: dashboard 540/540, shared UI 40/40, portal 87/87, widget 18/18; focused responsive tests 18/18; dashboard and UI TypeScript checks; dashboard, portal and widget builds; Panda codegen/cssgen; Park source/CSS guard 12/12 across 100 active files; Phosphor/editor guard 6/6; `git diff --check`. Synthetic Chromium at 320px reported no overflow for Filters/API Keys empty and error states or the Inbox; the default first Inbox row moved from about y377 to y242. Screenshots are under `/tmp/tocyn-inbox-after-*/`, `/tmp/tocyn-settings-routes-*/`, and `/tmp/tocyn-knowledge-inbox-visual-2026-09-20/`. These are supplementary, not native accessibility acceptance.
- The old disposable fixture was stopped and a fresh Node 22 `--local-beta` fixture started after rebuilding the local `better-sqlite3` binary for Node 22. Dashboard `/login` and API `/health` returned HTTP 200. A real password-plus-TOTP API sign-in for the synthetic operator succeeded, and authenticated `/api/tickets?limit=50` returned six tenant-A rows (the other two seeded rows belong to tenant B). A headless Chromium login/MFA journey reached `/inbox/all` without a page error; its screenshot is `/tmp/tocyn-auth-live-inbox-light.png`. No credentials or TOTP secret are stored here.
- A source audit found no active legacy `TocynButton/Input/Select/Textarea/Dialog/ConfirmDialog`, native visible controls, pseudo-Park markers, or Tailwind-only styling. The Tocyn logo, theme scope, and documented ShadowRoot-only `LUMINA_WIDGET_CSS` host opt-in are intentional. A Pin Input follow-up reproduced keyboard entry, macOS paste and autofill across all six cells; the initial one-cell Playwright observation settled after the Ark update and was not a durable defect.
- Signing remains an integration gate: current draft branch commits report unsigned (`%G? = N`), while the repository's active signature ruleset targets the default branch. Do not claim signed-main acceptance from passing PR CI. Authenticated Safari, native 200% zoom, reduced motion, VoiceOver, second assistive-technology combination, maintainer visual approval, #140 prerequisites and Gate A are open. No remote deployment or Beta.2 acceptance is claimed.

## Authenticated fixture and responsive correction, 08:22 UTC

- PR #316's previous exact head `994b846c` passed all eight required CI/CodeQL checks. The following work is a new local checkpoint pending commit, push and new exact-head checks; the PR remains draft. The unrelated dirty `codex/243-inbound-admission` checkout was not edited.
- Three bounded GPT lanes returned verified dashboard, portal and widget changes. The coordinator integrated an Ark Splitter CSS correction: the prior global `flex: 1 1 100% !important` made its desktop 32/68 ratio render 50/50. Authenticated browser now measures the controlled ratio and restores 34 after keyboard resize/reload. Ticket detail stacks a long reference above its heading at 320px; the heading no longer collapses to zero width. Flat Inbox rows, supplemental Park Collapsible filters and screen-reader-accessible mobile update dates retain queue behavior.
- A real authenticated synthetic fixture revealed that Tiptap emitted an empty update while switching plain/Markdown or mounting another ticket, overwriting a saved draft. The RichComposer sync fix has focused regression tests; live IAB acceptance now preserves a saved body through format switch, ticket switch and reload. The disposable test draft was discarded after verification. API tenant checks: A sees six tickets, B sees two, no overlap, and cross-tenant detail returned 404.
- Other scoped changes cover Knowledge table/category wrapping, API Keys cached-row retry, Ticket Detail email/body/attachment wrapping, portal long message/subject/recipient and error actions, and widget long title. Widget visual harness follows installed Park Field IDs and validates the approved Park focus geometry. Inter metric numerals now compute `"cv01", "tnum"` and `tabular-nums`; Atkinson remains the app body font. Source guards still reject active legacy `Tocyn*` controls, icon/editor libraries, pseudo-Park markers and Tailwind-only classes.
- Local checks against this dirty checkpoint: dashboard 547/547, portal 91/91, widget 19/19, shared UI 40/40, focused Inbox/RichComposer 56/56, portal retry 1/1; dashboard/UI/widget TypeScript and dashboard/portal/widget builds; Panda codegen/cssgen; Park guard 12/12 over 100 active files; icon/editor guard 6/6; specialist widget and ticket-detail browser scripts; `git diff --check`. Portal `tsc -b` run concurrently with Panda generation transiently reported shared type errors; the subsequent serial `npm run build --workspace=apps/portal` completed `tsc -b` and Vite successfully. Dashboard `/login` and API `/health` both return HTTP 200.
- Authenticated IAB evidence: Park sort menu opens and oldest-first persists after reload; full-email disclosure is honest about unavailable structured metadata; internal note and typed PDF attachment have correct labels; mobile list/detail at 320px is visible without viewport overflow. This is not native Safari or VoiceOver proof. The Mac remains locked for Safari/VoiceOver; an unlock request is pending. Native 200% zoom, reduced-motion/browser combinations, maintainer visual approval, signing on main and #140 release gates remain open. No remote deployment or Beta.2 acceptance is claimed.
- Ollama Local Granite returned an empty/Graphify-dependent proposal twice for a complete bounded VoiceOver checklist; the coordinator rejected it and took over. Hosted Graphify OAuth still reports invalid grant. No Cloud retry or paid fallback was used. Three GPT lanes continued contained source/visual audits; their returned text alone was not counted as verification.
- Next: finish coordinator source review, commit/push this coherent batch to PR #316, require eight new exact-head checks, update PR/issue receipt, then resume authenticated Safari/VoiceOver and #140 acceptance once the Mac is unlocked.

## Reference Inbox and cross-app reflow checkpoint, 08:43 UTC

- Previous PR head `42f505474a2ea5e9a3f67ed49d176bb13198def1` passed all eight exact-head CI/CodeQL checks. PR #316 and issue #315 received updated progress receipts. The unrelated dirty `codex/243-inbound-admission` checkout remains untouched.
- GPT Inbox lane compared the current toolbar/list to the maintainer-supplied HTML as a design reference. At 1280px the New Ticket Park button now has a visible label; at 320px it remains a 40px labelled icon control. The view trigger can shrink, and rows have separate rounded Park/Panda surfaces with 8px list gap/padding. Synthetic light and app-scoped dark browser checks at 1280/320 found no document overflow. Screenshots: `/tmp/tocyn-inbox-updated-1280-light.png`, `/tmp/tocyn-inbox-updated-320-light.png`, `/tmp/tocyn-inbox-updated-authdark-1280.png`, `/tmp/tocyn-inbox-updated-authdark-320.png`. The demo's custom shadows/hover transforms were not copied.
- GPT route lane corrected Usage metric-card reflow at 320px with full-width tabular readings below titles. GPT portal lane corrected a long unbroken populated ticket title that previously widened the 320px document to 1735px. The new specialist synthetic browser guard `tools/ui-browser/portal-detail-long-subject.mjs` passed in light/dark with reduced motion: document width 320, complete wrapping heading, intact feed and Back link, no writes/external requests/page errors. Receipt `/tmp/tocyn-portal-detail-long-subject.json`; widget and portal retry/empty audits also passed. These are Chromium/synthetic checks, not native Safari or spoken AT.
- Coordinator added MIME-first typed Phosphor attachment glyphs for PDF, image, ZIP, text and generic files while preserving the existing authorised download/preview path. The helper treats a conflicting specific MIME as authoritative and falls back to extension only when MIME is absent or octet-stream. Local Granite 4.2 3B returned a bounded proposal (677 input/375 output worker tokens); it covered happy paths but missed MIME conflicts, which the coordinator added and tested. A separate GPT review found no blocking icon/download/accessibility regression. Authenticated IAB showed `order-summary.pdf` with a decorative two-path duotone PDF SVG and the existing filename/size button label.
- Local verification after Panda codegen/cssgen (347 extracted files): dashboard 555/555, portal 91/91, widget 19/19, shared UI 40/40; affected TicketDetail/attachment focused 73/73; dashboard, portal, widget builds; shared UI/widget typechecks; Park source/CSS guard 12/12 across 101 active files; icon/editor guard 6/6; `git diff --check`. One initial focused `npx vitest` invocation at repository root lacked the dashboard workspace test setup and failed with ResizeObserver errors; the correct dashboard workspace invocation passed 73/73 and the full dashboard suite passed 555/555. No application failure was inferred from that runner mismatch.
- Next: commit/push this reviewed batch, obtain eight fresh exact-head checks, update PR/issue, then continue native Safari/VoiceOver and #140 acceptance when the Mac is unlocked. Maintainer visual approval, signing on protected main and product choices on organisation/tier filtering and quick-view sharing remain open. No remote deployment or Beta.2 acceptance is claimed.

## Park control and visual-regression checkpoint, 09:25 UTC

- PR #316's previous pushed head `76472c4d58f6a2d7ecdef8bb60edb66015ad515d` passed all eight exact-head CI/CodeQL checks. The following source is a newer local batch awaiting commit, push and fresh exact-head checks. The unrelated #243 checkout remains untouched.
- Auth browser checks at 320/1280 in light/dark found that six `md` MFA Pin cells intruded into the narrow card and nested `.dark` left Park plain text/icon aliases light. The installed Pin Input now uses `xs` below 640px without losing focus; the auth shell rebinds Park gray palette and six semantic icon aliases resolve in both themes. Compact navbar Avatar controls use official Park `2xs` sizing while retaining a 44px target; browser measured 56px inside the 64px sidebar, with status aligned and account/activity menus keyboard-accessible. Activity Popover's apparent left-edge position was only an opening-animation sample and was not patched.
- The Settings Park Menu now caps its height using Ark's `--available-height`; after reopening at 320x640, the last Channels row is fully pointer-accessible (y580–620) and its submenu remains in bounds. Knowledge Editor applies its existing Panda `minWidth:0` wrapper around Tiptap; a synthetic 176-character token now wraps in a 222px editor instead of expanding past 2,000px. A new specialist browser guard checks 320/1280 light/dark geometry. The widget preserves Park's 2px/2px focus outline and adds a 4px light backing only for its launcher, yielding a measured 18.90:1 visible band on a dark embedding host without leaving its ShadowRoot.
- The Phosphor regression guard is now TypeScript-AST-based and part of the ordinary PR test command. It checks active source, browser fixtures, manifests and build/PostCSS configuration for retired icon/editor references; direct vendor icons must be duotone and decorative or individually named, while static icon-only controls need accessible names through namespace, conditional and `asChild` cases. Peer review found and corrected initial false negatives/one false positive. No active retired `Tocyn*` control imports, pseudo-Park markers or unresolved semantic-token literals were found in a bounded source/CSS audit.
- Coordinator verification for this batch: dashboard 556/556 (before the one-line Knowledge wrapper, followed by focused Knowledge 13/13 and synthetic browser 4/4); portal 91/91, widget 19/19, shared UI 41/41; focused Layout 32/32, Settings 5/5, auth 6/6, Park guard 12/12 across 101 files, icon guard 14/14; Panda codegen/cssgen, dashboard/portal/widget builds, UI/widget typechecks and `git diff --check`. Browser receipts include `/tmp/tocyn-auth-colors-acceptance-audit.json`, `/tmp/tocyn-token-audit.json`, `/tmp/tocyn-settings-menu-after.jsonl`, `/tmp/tocyn-knowledge-editor-wrap-receipt.json`, `/tmp/tocyn-widget-visual.json` and `/tmp/tocyn-shell-matrix.log`. Synthetic Chromium and effective-viewport DPR2 checks are not native Safari zoom or VoiceOver evidence. Dashboard `/login` and API `/health` returned HTTP 200 during the batch.
- A bounded Ollama Local Granite guard-test proposal was truncated and contained incorrect accessibility expectations; no proposed case was accepted without coordinator verification. A bounded Cloud `gpt-oss:120b` guard proposal was also truncated and rejected. Three GPT lanes provided scoped source edits, peer review and browser evidence; the coordinator integrated and reran relevant checks.
- #140 remains OPEN with 13 listed prerequisites OPEN. The nearest independent dependency sequence after #315 is #130 live due-time snooze acceptance, then #137 routing against accepted queue state; #135 bulk behavior can advance in parallel. #132 still lacks full reduced-motion, focus-mode, zoom and fallback evidence. Native Safari light/dark, native 200% zoom, VoiceOver and second AT, maintainer visual approval, signing/default-branch conditions, product decisions on organisation/tier filters and quick-view sharing, and final #140 Gate A remain open. No remote deployment or Beta.2 acceptance is claimed.

## Knowledge retry and route reflow checkpoint, 09:42 UTC

- The preceding PR #316 head `cdd33737767705312452b7cd72b371df6e14a3b0` passed all eight exact-head CI/CodeQL jobs. This follow-on batch remains local until its own commit, push and exact-head checks; unrelated #243 work is untouched.
- The Knowledge Editor's article, category and form/save errors now have distinct sources. A late category-read failure cannot hide Retry article; after article recovery, Retry categories becomes available. A failed save has an honest heading, keeps the editable draft, and can be retried with Save Article. The specialist browser harness now exercises installed Park Select anatomy, Tiptap toolbar bold/Markdown, pending read-only behavior, failed synthetic save and identical retry payload. All browser API calls were intercepted and no external or unapproved writes occurred.
- Coordinator review and verification: focused KnowledgeEditor/KnowledgeLoad 16/16, full dashboard 559/559, dashboard TypeScript and production build, synthetic Knowledge browser harness pass, Park guard 12/12 across 101 active files, icon/editor guard 14/14, `git diff --check`. Panda codegen/cssgen ran as part of the build without a generated diff. Current local dashboard `/login` and API `/health` each returned HTTP 200.
- Independent synthetic navigation checks passed empty/failed/retry Inbox → Settings → Knowledge → Inbox at 320 dark and 1280 light, and rejected a stale tenant-A ticket response after switching identities. Six Chromium reflow checks at 640 CSS pixels/DPR2 in light/dark found no overflow or page errors, 40px targets, 2px/2px keyboard focus outlines, effective reduced motion, Atkinson UI fonts and Inter tabular metrics. Receipts: `/tmp/tocyn315-navigation-receipt.json`, `/tmp/tocyn315-zoom-focus-font-receipt.json`, `/tmp/tocyn-knowledge-editor.json`. These are synthetic browser checks, not native Safari zoom, spoken VoiceOver or backend authorization proof.
- A bounded active-source audit found no retired `TocynButton/Input/Select/Textarea/Dialog/ConfirmDialog/Panel/EmptyState` imports, JSX or exports, and no pseudo-Park markers. Remaining Tocyn-prefixed names are theme/logo contracts; `ParkEmptyState` and layout helpers are Tocyn Panda composites of installed Park components, not legacy native-control wrappers. No speculative deletion was made.
- The Mac reports locked to native computer control; Safari, native zoom, VoiceOver and second assistive-technology acceptance remain open. The maintainer's organisation/tier and quick-view semantics decisions remain pending. PR #316 remains draft; no remote deployment, release tag or Beta.2 acceptance is claimed.
