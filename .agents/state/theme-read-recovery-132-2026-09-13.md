# Theme restore read recovery — partial #132 / accounting #64

The observed local theme restore error was isolated to /api/settings/theme returning429 budget_exhausted while /workspace/theme-preference returned200. Current aggregate metadata had25,325 bytes headroom and3 live grants; no live reset or limit change was performed. The isolated regression establishes the missing database recovery opt-in: without it read33 fails after four grants; with it all40 reads finish,40 exact journals and2 acknowledged closures persist, and the second warm read adds no coordinator RPC. Session revocation returns401 without another journal.

One adapter argument enables the existing authenticated generic recovery contract; no guard, settlement, authorization or envelope is changed. The new test alone funds DO read/write limits at10,000 (20%=2,000 recovery; two768-unit closures require1,536). The unchanged older fixture's1,000 limits leave200 and cannot fund even one closure; that initial test setup failure was retained in diagnostic evidence. Adequately funded baseline still failed read33, excluding funding as the product comparison confound. Existing mutation/replay/permission and rollback native case passes. Two distinct native cases passed across focused runs;932 ordinary server tests, server/runtime types, scoped ESLint and diff checks pass. Missing jose on first setup was resolved using existing dependency symlinks, without installation.

Progresses #132; full acceptance and actual corrected browser replay remain separate. Candidate56640 is untouched. No new model calls, Copilot requests or paid fallback. Current Project percentage/baseline/forecast remain unchanged because this narrow correction does not establish a revised acceptance weighting. Root reviews before merge.
