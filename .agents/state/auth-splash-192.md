# Public authentication layout #192

Owner requested a separate narrow UI delivery based on three illustrative Gemini files and ten supplied WebP assets. Illustrative passwords for customers, hardware-key support, simulated authentication, credential logging and mobile splash placement are not adopted. Existing customer magic-link/OTP and operator password/TOTP APIs remain unchanged. MFA enrolment remains on its existing route; no backend/authentication migration is introduced.

Branch `codex/auth-splash-login`, worktree `/private/tmp/tocyn-auth-splash`, builds on merged branding #186/PR187 (signed commit f5f19f28de4eb6e9df08920270e13c0ee9445c7f). Shared layout uses existing headless/native Tocyn controls, current root theme (system fallback before sign-in), one random image choice per mounted visit, top-left object positioning and 45% maximum panel. Below768px the image element is omitted, with no splash download. Login steps retain the image while correcting inputs. Customer challenge persists in browser history state for reload without putting it in the URL. Existing /verify magic links and /mfa direct redirects remain supported.

Local previews: operator http://127.0.0.1:5197/login and customer http://127.0.0.1:5174/login, existing local backend8787. Other servers untouched. Owner approved the remaining plan with the 45% correction; deployment is local-only because no hosted target was specified.

Validation: Node22 dashboard/portal builds, shared UI typecheck,261 dashboard tests,69 portal tests and21 shared UI tests, portal/shared-layout lint,8 Chromium visual variants pass. Three real local two-tenant fixture tests pass. The full local portal Wrangler rehearsal could not start because owner8787 is occupied; do not stop that server or claim rehearsal success. Required CI will repeat it in an isolated runner. Dependency audits report0 vulnerabilities including production-only. Initial lint command used a nonmatching config; corrected portal and layout checks ran with portal config. Local visual script is tools/ui-browser/auth-splash.mjs; screenshots are disposable/private/tmp files. No credentials or customer data recorded. Customer preview moved from5198 to the free canonical5174 port so existing captured login links retain their approved origin. Use the existing tenant entry link/public routing key for authentication; bare /login is a visual preview.

Owner explicitly requests one narrow Copilot review after stable checks: authentication/layout changed files only, no repository-wide audit or repeated run. GitHub PR review can inspect supporting context; no file-access hard limit is claimed. Standing owner review bypass does not waive checks. Final PR/merge/signature/review/deployment evidence still required. No subagents contacted or launched in this side conversation.


Final review corrections: one owner-requested Copilot Lite review completed on PR #193; no second request. Addressed its five observations together: all auth motion/transition suppression, defer splash mounting until outgoing theme cleanup, preserve visual choice through MFA enrolment navigation, customer OTP reload regression, and corrected preview references. The form logo is centered; splash remains top-left at up to45%. Reuse the shared theme tokens and explicit logo theme adapter; no user-avatar change. The portal CSS budget remains6000 gzip bytes, unchanged; simplified redundant auth styling to satisfy it. Mobile omission is controlled by the shared media-query subscription, so no hidden image is downloaded.

Integration owner must verify final PR #193 checks, signed merge and local previews, then close #192 and synchronize Project completion. The issue completion receipt is the authoritative final merge/check/deployment record; no further feature work is delegated from this side conversation.
