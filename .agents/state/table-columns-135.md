# #135 table column preferences — partial delivery

The coordinator retained the approved #135 scope and reviewed the column implementation proposed by the GPT-5.6-Luna execution worker. The work adds per-operator column order and visibility; reference remains mandatory at any position and always opens its conversation. Density continues to use the existing preference. Bulk assignment/state/snooze/tag actions and their full acceptance remain outstanding.

Migration0079 only adds bounded `table_columns` JSON to existing version2 presentation rows. The existing tenant/user key, foreign key, revision, timestamp and scalar preferences remain intact. Legacy version2 writes omit the field without replacing an existing selection; old responses without the field receive the six-column default. Malformed present values fail validation. Presentation writes reserve an additional512 D1 storage bytes for this field; other resource dimensions are unchanged. This is not whole-operation physical storage clearance.

Coordinator corrections: allow reference in any position, normalize legacy save responses, validate repository input before mutation, display controls in stored order, keep a navigation link when subject is hidden, and preserve existing column content/styles. Additional coordinator tests exercise populated migration preservation, malformed columns, non-leading reference, legacy response normalization and remount persistence.

Verification on13September2026:47 focused dashboard tests passed against the integrated main revision faa78cf5 (preferences and persistent Inbox);16 Node22 SQLite/HTTP fixture tests passed (migration and presentation/theme). Earlier worker server/dashboard typechecks passed; exact published revision must additionally pass required CI/security. Browser column configuration and spoken accessibility acceptance are still pending. No release or issue completion is claimed.

Routing: coordinator owns migration/security, integration and acceptance; Luna drafted/applied the bounded column code. Free Ollama gpt-oss:120b separately returned a proposed bulk-selection matrix (655 input/838 output worker tokens, non-truncated). The coordinator rejected its incorrect partial retention on filter change and its conflation of revision rejection with unknown commit, plus unproven cancellation/copy claims. That proposal is not implemented or executed evidence. Local Granite last refused the approved4GiB memory guard; the guard was preserved. Worker token counts are not total usage or measured savings.

Next: signed PR313 publication and required checks, actual table preference persistence/keyboard acceptance, then complete the remaining bulk work against the current approved issue contract. Keep #135 in progress and preserve its baseline dates; no evidence supports100% completion.
