# #64 HTTP read completion — partial delivery

Base: signed434e58d6. Branch: codex/64-sustained-reads. Native GPT worker owns this bounded read-completion increment; coordinator owns later generic recovery. Context: direct verified source for known four HTTP ticket detail/history paths; no independent Graphify crawl or model calls.

The native32-read acceptance finding showed that sustained-use recovery cannot infer completed read work from a response. This increment retains the admitted authority and adds exact durable completion evidence before returning successful dashboard/portal detail/history responses. Its D1 batch rechecks current principal, session, expiry, ticket visibility and exact budget snapshot, then writes the existing content-free grant-operation link. Unconfirmed business/fence/acknowledgement results settle unknown and remain charged. It adds512 read rows and16 write rows to the prepaid envelope, covering the measured bounded completion batch.

Validation: server TypeScript and focused ESLint passed;54 dashboard/customer handler unit tests passed;8 native Worker/D1 tests passed. Native tests cover all four successful response paths and their independent ledger links, unchanged private/public projection, final group/session/customer-ownership/policy changes, and a lost completion acknowledgement retaining its committed link without returning response content or fabricating closure. Native completion batches fit the added read/write allowance. Existing long-history boundedness still passes.

Remaining: generic principal-aware exact closure/reconciliation, bounded holder recycling, expiry behavior and sustained-use demonstration. The four-refill limit and grant expiry rules are unchanged. This is Progresses64, not completion or acceptedBeta.2. Preserve current Project dates/progress pending evidence-based weighting. No provider activation, remote runtime, production data or local fixture reset is part of this change.

## Combined workspace and summary lifecycle slice

The coordinated draft also fixes existing workspace read/write completion accounting: one ledger/settlement per logical request across bounded retries; failed dispatch/parsing outcomes settle unknown; workspace read cleanup allowance covers measured work; summary SLA allowance includes its actual completion writes. This reuses the existing application flows and does not itself implement grant recycling.

Combined validation reported by the two native workers:764 server unit tests; server TypeScript; scoped ESLint;19 workspace native tests,1 summary native test,8 ticket-read native tests. Workspace retry uses4 batches but one ledger/settlement,18 writes within20; cleanup/list209 within272;11 requests settle exactly once (6 committed/5 unknown). Summary SLA uses4 writes within16. The ticket runtime test's typed result predicate was corrected after separate runtime typechecking reported implicit-any. Root's unvalidated coordinator/cache/closure edits are excluded from this draft snapshot.


## Coordinator continuation — 13 September, pending integration

The preceding paragraphs describe signed draft269 at de10f5e5. The following work is a later working-tree increment; its final signed revision and required CI are still pending.

The coordinator implemented cold-path recovery for opted-in current staff/customer credentials. At four unreconciled blocks, the cache serializes one exact quiescent grant closure with a freshly charged allocation. Only confirmed central reconciliation returns a local holder/refill slot; unknown or active operations never seal. Recovery has a separate recovery-purpose envelope. A fresh authority resolution after recovery prevents allocation from replaying an older authority timestamp. Warm reads still use their existing credential checks and no per-read DO call.

Expired quiescent grants may retire metadata only with their entire original envelope retained as uncertain and no measured amount/refund. This is an explicit certificate mode. A live certificate that crosses expiry is not silently upgraded, and missing central metadata is never evidence of success. Two failed acknowledgements retain local capacity. Consequently this is bounded sustained-use recovery, not an unlimited-availability claim.

Independent GPT review identified cleanup that could remove expired but unconfirmed closure evidence. Migration0073 adds nullable reconciliation acknowledgement metadata and a partial indexed cleanup lookup. Only exact centrally acknowledged closures are marked; bounded cleanup leaves unconfirmed operation journals intact. Existing rows remain unacknowledged. Production migration/runtime clearance is separate.

Current checks:12 pure coordinator tests;64 consecutive staff detail reads and64 customer detail reads, each retaining at most4holders/32operation entries;7 spaced reads across expiry;8 concurrent cold reads share one closure/allocation;one lost central acknowledgement retries successfully;two losses retain slots;four grants with unknown completion never reconcile. The list worker additionally verified64 staff and64 customer list responses with exact durable links,4closures and12reservations (8work+4recovery), with no warm per-read DO calls. Cleanup has3 focused native checks including legacy rows, partial-index search and retained unconfirmed evidence. Full existing budget-admission native suite passed89/89 after core/cleanup changes. Wider final affected validation remains pending integration.

The list lifecycle increment retains authority through the current credential/population/policy transaction and settles once after response construction. Activity admission is being implemented against a bounded summary projection and remains pending its own native validation. Existing configuration, saved-filter, channel and group adapters have been opted into the same recovery callback because their exact durable links and settlement already exist; their focused checks are pending.

Resource routing: three native GPT workers handle bounded implementation/evidence while the coordinator retains policy, concurrency and release decisions. Hosted Graphify navigation at indexed226ad24d was verified against local source; clipped retrieval used focused symbol search. Local Ollama previously refused its connected6GiB memory guard and was not forced. A free cloud gpt-oss:120b test-scenario proposal used643input/700output tokens and was truncated; it contradicted the supplied no-slot-release-before-reconciliation contract and was rejected. No claimed token savings, paid fallback or Copilot review.

Next: finish activity checks, integrate latest main including #130 queue counts without losing its analytical envelope, run affected checks on the combined revision, update draft269 with a signed checkpoint, and validate a new local preview only after checks pass. #64 and Beta.2 remain incomplete; inbound-email orchestration, remaining workspace acceptance and production/runtime gates remain independent.


## Integrated validation — 13 September, 12:43 UTC

The combined 47-file change is integrated on signed main84d7295704330db1413e9d5f51de3a3f4ead45f0. Coordinator resolved three overlap conflicts while preserving the merged queue predicates/counts and their analytical reservation. Source tree before this receipt update: e02b9eaebf9222a519118c98a9799354311d8bad.

Activity reads now execute current recipient/policy fencing, both bounded projections and the exact completion ledger in one D1 snapshot. Transitions fence and journal in their existing mutation batch. Presentation subjects are bounded to512Unicode characters plus ellipsis; stored subjects are unchanged. Native activity checks include sustained64reads, authority/group/policy changes and unchanged legacy pagination. Configuration, filters, channels and groups each passed40read recovery demonstrations. Stable-binding instrumentation uses request-local AsyncLocalStorage counters. The existing two-attempt same-operation guard is retained; a third same-key channel replay still rejects without extra mutation or coordinator call.

Final integrated serial native run passed54/54 across seven suites: ticket-list admission and sustained reads, queue counts, ticket detail/history, activity, workspace, and SLA summary. The earlier full core budget runtime passed89/89 including confirmed-only cleanup. Server ESLint exited0. An initial workspace-unit run used incompatible Node ABI147 against the installed SQLite ABI127; no dependencies were rebuilt. With existing Node22.19.0, all765server unit tests passed. The initial dashboard run passed326/327 with a focus timing failure; a focused Node22 rerun passed9/9, and the full Node22 dashboard rerun passed327/327. No dashboard source was changed; the original focus failure root cause is not established. Portal69, widget11 and shared UI21 tests passed in the original run. Source was unchanged during these checks.

Local Granite remains unavailable at its intact6GiB guard: observed4.54GiBavailable,1.46GiBshortfall. Free cloud produced a compact synthetic #137capacity fixture (486input/450output tokens); coordinator checked its expected tenant-specific loads and forwarded it to the ownership worker. This is proposal usage, not executed tests or measured ChatGPT savings.

Next: sign and update existing PR269, require exact-head CI/security gates, then launch and verify a clean signed local preview with the same64-reservation policy. #64 remains partial; this increment does not clear all metered paths, #243orchestration, #137routing, end-to-end workspace acceptance or #42production gates. Original baseline/Project progress remains preserved until broader acceptance weighting is defensible.
