# Knowledge read recovery — partial #134 / accounting #64

Repeated admitted knowledge lists reproduced429 budget_exhausted on read33. The adapter already settled exact operation journals but did not supply its database for existing authenticated session-grant recovery. This adds only that capability; default four-refill/eight-operation guards, authorization, envelopes and live policies are unchanged.

The existing adequately funded isolated fixture now completes40 lists with40 exact operation journals,4 acknowledged closures,9 refresh/9 reserve/4 reconcile RPCs, and no extra RPC for the second warm read. Its unchanged50M DO limits reserve10M for recovery, above4×768=3072; no test or live policy increase. Initial post-fix failure exposed the fixture's namespace wrapper omitting reconcileFromTrustedAuthority; transparent forwarding/counting was added. The baseline lacks the recovery opt-in and cannot invoke that method, so its33-read failure remains the meaningful comparison.

Eleven distinct native cases pass across runs: ten existing list/detail/content/R2 counter/authority/tenant/low-budget negatives and the new40-list case.932 ordinary server tests, server/runtime TypeScript, scoped ESLint and diff checks pass. No duplicated40-case content families, new browser run, AI/provider call or current candidate56640/54737 request. This is bounded evidence, not unlimited-read or full #134 acceptance.

Progresses #134. Zero Copilot/model requests; manual triggers checked. Project weighting/baselines preserved. Root retains review and exact-head merge gates; combined candidate update remains queued.
