# #70 current integration candidate — 11 September 2026

Current integration: `231a153` includes accepted recovery main `ab8759d`; application UI/AT revision `e538af9` includes branding, with scoped accessibility receipt `10e38ee`. Repository-boundary correction `3f30f10` moves unchanged typing authorization SQL into its repository. Root owns integration and issue acceptance. Earlier sections below are historical development checkpoints, not outstanding instructions where this paragraph supersedes them.

The candidate implements shared advisory typing/presence, atomic draft/conversation preconditions, receipt-safe lost-response retry, explicit refresh/review/rebase/manual-send, and bounded multi-page review. Revision reads bracket conversation refresh; when ascending pages remain unloaded, rebase is unavailable until the operator loads and reviews the later material. No automatic send or draft discard occurs on conflict.

Latest checks: 46 focused dashboard tests; combined native collision/runtime types and production-browser proof; fresh Node 22 repository gzip-level-9 build at e538af9: total JS 569435/570000, initial 101086/135000, CSS 15644/16000. The former default-level-6 overrun is superseded measurement error. Full exact PR CI/security is pending.

Safari/VoiceOver evidence is in `docs/security/evidence/collaboration-70-safari-voiceover-2026-09-11.md`. The final branded journey exposed separate rebase/manual actions; spoken output after rebase was “Draft saved.” and after send was “Showing 2 messages. All messages are loaded.” Longer action notices were accessibility-tree visible, not separately captured as speech. Earlier exact stale/review/lost-response speech retains its stated revision. Do not broaden these observations into full #70 acceptance.

The manual fixture uses a 30-minute budget window established before admission, preserving ceilings, recovery partition and the 60-second grant lifetime. Native evidence proved direct combined mode succeeds while a genuinely expired window rejects with zero grants/articles. An intermediate API-only transition is not required for combined activation.

Remaining #70 work: durable authorized mentions through #133, interruption/motion preference integration through #132, remaining private colleague workflow and final combined accessibility/tenant/resource acceptance. Typing resource estimates do not prove full #64 active admission. Keep #70 and #140 open. No Copilot review or remote/provider action.

## Durable internal mention slice — local candidate

`codex/70-durable-mentions` composes a selected, normalized set of at most 16 UUID staff recipients only into an internal dashboard reply. The versioned reply capability advertises `internalMentions: { version: 1, protocol: 'internal-activity-v1', maxRecipients: 16 }` only on the current combined staff route, which accepts and atomically enforces the field. Legacy/off and API-only routes neither advertise the capability nor accept the request field. The dashboard keeps selected recipients through a rejected commit or retry, includes them in the exact semantic idempotency intent, and clears them only after the winning note is confirmed; it never parses or stores note body text in activity facts.

The staff service builds #133 `OperatorActivityRepository.prepareTrustedAppend` statements with only the winning canonical article ID. It appends them before the staff receipt so a duplicate/lost-response winner replays without new activity, and an activity trigger checks current recipient tenant/ticket/group access in the same D1 batch. Public payloads are rejected. A revoked or cross-tenant recipient yields retained-draft `409 mention_recipient_unavailable` with no article, audit, SLA, receipt, or activity partial write.

Focused Node 22.19 synthetic Miniflare proof covers a private winner, public rejection, tenant denial, final-batch group revocation, receipt replay, changed-recipient idempotency conflict, and a full 16-recipient projection. The observed base versus full batch was 10/27/25 versus 26/123/121 statements/rows-read/rows-written: 16 additional prepared activity statements and 96 additional observed D1 writes. The accepted combined worst case (16 mentions, 10 attachments, acknowledged draft precondition, audit, local-beta assertion and receipt) measured 38/153/153. It disproves a 128-write estimate, but remains within the existing 1,024 per-attempt canonical reservation and 2,570-read staff envelope; the service already reserves the conservative two-attempt 2,048 write total. A concurrent same-key worst case made two canonical attempts but left one article, ten attachments and sixteen activities, then replayed the existing receipt without fanout. These are local D1 observations, not provider billing, CPU, bytes, or whole #70 resource evidence. Focused dashboard capability/workflow tests and a production dashboard build passed under Node 22.19 after an ignored worktree-local dependency alias overlay. Full #70 remains open.

## Historical development checkpoints — superseded by current candidate above

# #70 collaboration delivery

## Current partial candidate — 11 September 2026

Root is integration/acceptance owner. Native Terra/high implemented frontend/protocol work in `codex/70-private-collaboration`, based on accepted #68 main `8987f7c1b63349efe78ce1ddeedd5530d7e5c779`. Application candidate `2f06009b9aa1de1de640cd78875ac912133a0bb9` is not full #70 acceptance.

One dashboard CollaborationProvider reuses the existing realtime connection for Layout and ticket detail. Bounded versioned typing hints carry ticket/revision/active only; the server must derive identity and tenant. Received hints are capped at 128 and six seconds, cleared across hidden tabs, reconnect/session changes and explicit completion/discard/exit. Stable callbacks prevent unrelated broadcasts from triggering location cleanup. Static readable text avoids unsolicited announcements; cognitive/interruption preference integration remains with #132/#70.

Validation: dashboard 39 files/265 tests, typecheck and production build passed. Root rebuilt with the CI manifest on Node22.19.0: 568,328 total and 100,901 initial gzip JavaScript bytes, below unchanged 570,000/135,000 ceilings. This is resource size evidence, not final controlled browser timing or assistive-technology acceptance. Full/prod dependency audits reported zero known vulnerabilities. Existing local Terser was linked only into this worktree; no paid/remote work.

The next bounded server slice is local and unpushed on the same branch. `NotificationDO` accepts only the versioned client ticket/revision/active payload, derives the actor from the live staff session and authorizes the sender plus every recipient against the current tenant ticket and group membership. It limits each transient connection/ticket cadence to one update per second, retains no typing state across close, reconnect or hibernation, sends a six-second server expiry, and remains within the existing 128-socket ceiling. A connection keeps at most 16 unexpired ticket keys; new keys are rejected rather than evicting a valid throttle, then recover after the six-second transient window. The new disposable Miniflare proof exercises two tenants, authorized and denied same-tenant recipients, a customer denial, malformed input, membership revocation, throttle, expiry and reconnect; the existing 128-capacity native proof also passes. The complete 12-test local storage/background runtime command passes on Node 22.19.0 using the declared Zod 3.25.76 through an ignored worktree-local `apps/server/node_modules/zod` alias; the earlier Zod 4 resolution was dependency setup drift, not a #64 resource defect. A separate typing envelope preserves the generic #64 notification envelope and conservatively reserves 384 D1 rows and 16,385 potential diagnostics for one accepted grouped frame; its cleanup-inclusive form reserves 33,152 D1 rows and 2,113,793 potential diagnostics. A native 128-session mixed fixture observes all 384 authorization queries, 320 returned rows because 64 denied memberships have no row, and delivery only to the 63 authorized recipients. These are local reservation and diagnostic ceilings, never a provider billing, CPU/duration, byte-transfer or complete event-metering claim. It is advisory only and does not create typing replay or a second realtime store.

## Remaining implementation and integration

- Root must refresh this partial branch on its later accepted #64 integration identity before final merge review. The NotificationDO typing path is now implemented locally, but its broader #64 resource-envelope ownership and staff integration remain with root.
- Reply precondition must use existing draft `baseConversationRevision` and canonical `conversation_events`, atomically rejecting material new content before mutation/audit/email. Current audit kinds include `ticket.intake` and `message.reply`; do not confuse them with automation event names. Define the material-event comparison explicitly, not a second revision store.
- Conflict must retain the draft and require review, without automatic resend. Completed receipt replay precedes a new precondition check because the original successful reply advances the conversation.
- Internal mentions must create authorized durable activity through staged #133 backend work; avoid a whole-issue #70/#133 cycle.
- Finish two-session actual runtime/browser, negative tenant/group/revocation, failure/recovery, bounded resource, keyboard/VoiceOver/contrast and #132 preference integration evidence.

No presence lock or permission grant, autonomous send, customer disclosure, provider activation or beta.2 readiness is claimed. #140 remains the final gate. Project start receipt: https://github.com/nathcymru/Tocyn/issues/70#issuecomment-5630649116. Root owns GitHub/Project completion and forecasts. Zero Copilot requests.

## Reply-precondition backend slice — 11 September 2026 (local commit pending integration)

The #70 worktree now has a bounded canonical seam for the staff-ticket worker: `TicketMutationReplayRepository.commitStaff(candidate, staff, precondition?)`, where `StaffReplyPrecondition` is `{ ticketId, generation, revision, baseConversationRevision }`. Existing two-argument callers keep their behavior. The canonical helper derives mode, body, body format and ordered verified attachments from the candidate and combines an exact `operator_drafts` check with the current staff transaction assertion. It accepts metadata-only events but rejects a later `ticket.intake` or `message.reply`; the D1 batch cannot proceed when the assertion fails. A single bounded reread distinguishes that retained-draft conflict from an unavailable database. The pending staff service must pass an acknowledged precondition, include it in the idempotency fingerprint, retain the draft on `StaffReplyPreconditionConflictError`, and map that conflict to 409 only after its existing receipt-winner reread.

`0039_staff_reply_precondition.sql` adds `(tenant_id,ticket_id,kind,sequence)` to support those material-event maxima. Operator workspace now exposes `POST /api/workspace/drafts/:ticketId/rebase`, which preserves the exact saved body/format/mode/attachment sequence and atomically requires the caller's reviewed full conversation revision to still equal current `MAX(sequence)` before it updates the exact generation/revision draft. It never jumps to a newly observed revision. The route is local-beta admitted as a conversation write. No dashboard client is wired yet.

Focused Node 22.19.0 synthetic Miniflare evidence: server typecheck; `test:operator-workflow` (six tests, including metadata/new-material/CAS/48h rebase); `test:local-beta` (12); `typecheck:staff-ticket-mutation-runtime`; and `test:staff-ticket-mutation-runtime` (including tenant/actor/metadata/48h precondition checks) passed. This is not full end-to-end stale-reply evidence until the staff service/type/handler worker passes the precondition and error mapping, nor #70 completion.

## Collision integration slice — 11 September 2026 (local commit pending root review)

Merged reviewed staff branch `2259eb1` locally and added the end-to-end collision contract. `reply-capability` advertises `collision: { version: 1, protocol: 'draft-precondition-v1', conversationRevision }` only in the server-enforced combined admission mode and only after current ticket/group authorization. Legacy/API-only reply schemas remain unchanged and cannot claim collision safety. The staff service includes an acknowledged `{generation,revision,baseConversationRevision}` only when supplied in its normalized semantic fingerprint, passes it after attachment verification to `commitStaff` as the optional third argument, preserves receipt-winner replay before mapping a canonical stale conflict to `409 staff_reply_stale`.

The ticket composer uses one idempotency key per exact acknowledged draft intent (generation/revision/base/mode/body/format/ordered attachments), retains it across uncertain 503 retries, retains the draft on failure, and sends the draft precondition only when capability is advertised. A 409 presents explicit Review and rebase. That action refetches the server-derived full current revision, uses the exact draft CAS rebase endpoint, does not send automatically, and refuses to overwrite text edited during the request. A successful explicit rebase makes a new intent/key; draft cleanup remains receipt-confirmed.

Validation after merge: Node 22.19 server typecheck, dashboard handler 22 tests, dashboard workflow/controller 65 tests, focused new native stale-precondition test, and the complete staff mutation native suite all passed. Dashboard production build produced 566,274 total gzip JavaScript bytes and 100,991 initial gzip JavaScript bytes (both below 570,000/135,000 limits); it uses the worktree-local Terser symlink only. This is synthetic/local evidence. Remaining #70 limits are durable #133 mentions, #132 preference integration and actual assistive-technology acceptance; combined local-beta end-to-end browser fixture still needs root’s guarded-beta sequencing and no full #70 completion is claimed.

## Guarded combined collision proof and stale-review correction — 11 September 2026 (local commit pending root review)

The stale reply UI now owns a typed review state instead of incorrectly relying on a draft-save conflict. A `409 staff_reply_stale` preserves the draft, blocks another send, and offers an explicit **Refresh and review conversation** action. That action fetches and renders the current ticket first, then records the freshly advertised server-derived full conversation revision. A distinct **Rebase saved draft** action sends that exact revision to the draft CAS endpoint; it never auto-sends and leaves newer local text intact when its version races. Only after rebase can the operator deliberately send a new acknowledged intent/key.

`LocalTenantFixture` now has a fixture-only guarded admission transition: off, API-only, then combined, backed by its real ephemeral Miniflare D1/R2 and BudgetCoordinator/GrantHolder Durable Objects. It seeds a bounded two-tenant authority with resource limits sized for the existing conservative staff envelope; this neither changes the beta.1 contract nor disables local-beta authorization. `combined-collision-runtime.test.ts` proves off/API-only capability omission, combined capability, two tenants plus a second current same-tenant agent, material stale denial with no article, retained draft, explicit rebase, and manual success. `ui-combined-collision-browser.test.ts` runs the production dashboard build through an owned ephemeral loopback server and real guarded Worker routes: it renders the intervening colleague message before rebase, preserves draft text, manually sends after CAS rebase, then injects one lost response only after a real `201`; retry uses the identical UUID key and produces one stored article. It makes no external requests and does not use shared local ports.

Focused Node 22.19 evidence: `typecheck:local-tenants`, `typecheck:combined-collision-runtime`, `test:combined-collision-runtime`, `typecheck:ui-combined-collision-browser`, `test:ui-combined-collision-browser`, and dashboard `TicketDetailWorkflow` (41 tests) passed. The rebuilt production dashboard is 566,573 total gzip JavaScript bytes and 100,990 initial gzip JavaScript bytes, below unchanged 570,000/135,000 limits. This is local browser automation, not VoiceOver/Safari or deployed-runtime acceptance. Durable #133 mentions, #132 interruption preferences, and actual assistive-technology evidence remain outside this partial #70 slice.

## Coordinator validation update

PR189 lint at883a6e9 found raw D1 access in NotificationDO. Correction3f30f10 preserves the current tenant/ticket/group query and independent resource emitter inside a repository. Scoped lint, full server types and13native storage/background tests passed. Integration231a153 preserves the optional staff precondition alongside accepted API grant operation links. The combined collision native proof passes; the full integrated budget suite passed 82/82 under completed local handle 85987. Exact PRCI must pass after this batch.

Recovery194 is accepted at signedab8759d, matching tree9d5a7112dd326530c0c8db70be8421d172a979ac and reviewed6e0440a/testedc894679. CI34584188793 andCodeQL34584186132 passed; no unresolved threads; standing approving-review-only bypass recorded in issue64 receipt5632583296. Watch85459 completed and was reaped. Customer195 is now based onmain atc03a2da with CI active. WikiHome source update here is pending publication after integration. Next full human update11:09BST, following10:39 status.

## PR #189 operator-activity fixture compatibility correction

At PR #189 revision `d33ba28`, the activity fixture helper creates synthetic agents at session version 1. Updated only `scripts/operator-activity.test.ts` to use that current version for the baseline recipient and cursor cases; membership removal advances it to version 2, and the final explicit revocation advances it to version 3. The tests retain stale-session denials and use current credentials for the distinct actor/tenant/key cursor-negative cases.

Validation on Node 22.19.0: `npm run test:operator-activity` passed 5/5 (the native 1,200-row cursor proof reported 19 pages, 800 authorized items, 304–606 D1 rows read per page); `npm run typecheck` passed. This local correction has no remote action; root owns integration.

### Current-main size-gate correction — 11 September 11:11 BST

Accepted login integration #193 increased dashboard total gzip JavaScript to 570,231, above the unchanged 570,000 gate; CI build at 8b56af0 failed that exact check while the other reported performance checks passed. Adding the existing SLA settings screen to the existing lazy administration chunk removes redundant chunk overhead without changing routes, features or thresholds. A fresh Node22/Vite8.2.2 manifest build at gzip level9 measures total JS569,716, initial JS101,803 and total CSS15,850 (limits570,000/135,000/16,000). Required exact-revision CI/browser checks remain pending. Logs: `/private/tmp/tocyn-189-build-8b56.log`, `/private/tmp/tocyn-189-admin-settings-build.log`. A prior trial grouping CollaborationContext with hooks had no output-size effect and was fully reverted.

## Durable-mention draft continuity correction — 11 September 2026 (local commit pending root review)

`0042_operator_draft_mentions.sql` adds bounded JSON `mentioned_user_ids` to the existing 48-hour `operator_drafts` record. The workspace save/get/rebase path validates, normalizes and returns no more than sixteen internal UUID recipients; rebase preserves that exact selection. The dashboard keeps selection in the acknowledged draft instead of ephemeral component state, so reloads, explicit rebase and uncertain-response retries preserve the intended recipients. The reply's idempotency intent and canonical precondition now compare the exact normalized recipient selection as well as mode, body, format and ordered attachments; a different selection is a retained-draft stale conflict, not a changed send.

The recipient picker no longer slices the existing server-bounded agent roster at sixteen. Eligible colleagues after the first sixteen remain keyboard-discoverable, while the selected set remains bounded at sixteen. Capability omission still hides the picker and sends no mention field; recipient revocation remains an explicit retained-draft failure with no automatic recipient removal or delivery claim.

Focused Node 22.19 synthetic evidence: dashboard `TicketDetailWorkflow` 46/46 (reload/lost-response same-key and seventeenth colleague), `OperatorDraftController` 25/25, server `test:operator-workflow` 6/6, server workflow and staff runtime typechecks, and `test:staff-ticket-mutation-runtime` 28/28. The staff runtime observes the deliberate five `conversation_events` indexes (two unique keys plus article, operational-metric and material-reply indexes), proves the material reread query is indexed without a scan/sort, and retains the 1,024-row canonical-attempt ceiling. This is local synthetic evidence only; full #70 preferences, broader AT and integration acceptance remain pending.
