# Issue #132 preference recovery — 13 September 2026

Bounded execution agent worktree: `/Users/ty/Documents/Tocyn-worktrees/preferences-132`; branch `codex/132-preference-recovery`, based on accepted `c0964779`. Reused coordinator's verified file packet and direct source reads; no separate Graphify crawl or worker inference. Coordinator retains integration and consequential boundary review.

Two reproduced defects corrected in `useOperatorPreferences`: React StrictMode effect replay retained an obsolete restore promise and left preferences loading indefinitely; editing after an invalid restore hid Retry and exposed a Save action that could not write without a known revision. Cleanup now detaches obsolete flights, and unvalidated preference previews retain their recovery status/error.

Validation: before correction, three regression assertions failed (including the strengthened old-schema test); afterward, 14 preference-hook/theme-provider tests passed and dashboard TypeScript passed on Node 22.19.0. Added synthetic server-response coverage for saved-preference remount restoration and late old-response rejection across user and tenant changes. These are frontend contract tests, not actual server isolation or complete browser reload evidence. No new dependencies, ABI change, preview restart or shared-page edit.

Next: coordinator reviews/signs this patch and delivers through the issue PR. #132 remains open for full workspace integration, real persistence/reload, focus-mode critical-state coverage, 200% zoom/browser text scaling and complete reduced-motion evidence. Read-only inspection also found a similar retained-flight lifecycle in `useOperatorTheme`; coordinator notified for separate verification. No issue closure, arbitrary progress percentage or B2 acceptance claim.

## Coordinated theme follow-up

Coordinator explicitly assigned the analogous theme lifecycle verification. The new StrictMode theme regression first failed with appearance stuck at `loading`; cleanup now detaches obsolete theme flights and queued operations, while stale save completion cannot operate a newer queue. The application already mounts under StrictMode, so this restores the existing development entrypoint rather than adding a new mode.

Consolidated validation: 29 preference-hook, theme-hook and provider tests passed; dashboard TypeScript and diff whitespace checks passed. Existing JSDOM navigation diagnostic remains in the theme suite; no test failure. No server/auth/provider/shared-page edits or preview interruptions. Both lifecycle corrections and malformed-presentation-preference recovery are ready for coordinator signing/integration. Actual integrated browser and accessibility acceptance remains outstanding.

## Delivery preparation

Current #132 scope/dependencies were reread. Live open PR inventory contained only #261, so there is no competing preference PR. Inspected live rulesets: 22454811 contains deletion/non-fast-forward rules only, no Copilot review rule; 22415946 retains PR review/thread-resolution requirements and the administrator PR-only bypass. Checked-in Copilot setup workflow is manually invoked; no automatic review request trigger was found. Required CI/security and signing rules remain active. No review requested and no gate modified.

Preference save finalization already changes `saving` only when its promise is still current, with no queued action afterward; unlike the theme finalizer it has no obsolete-flight side effect to fix. Response handlers remain epoch checked. No speculative change or redundant test added.

Coordinator PR body is available outside tracked sources at `.agent-context/preferences-132-pr-body.md`; five tracked/additional delivery files remain ready for GitHub signing. Final local validation remains 29 focused tests plus dashboard types, without repeated successful runs. Use Progresses #132 and preserve open acceptance gates.
