# Bounded global search types — partial #131

Current approved scope permits customers and knowledge where available without a new search engine or broader access. Root accepted selectable Tickets/Knowledge/Customers, with Customers explicitly unavailable because existing users.manage directory pagination is not a customer query contract. No first-page directory search or new backend is introduced.

Tickets query the existing authorised endpoint in a bounded panel, never the legacy route. Searching, clearing and type changes leave the current work view and persisted filter intact. Filter this view remains separate. Knowledge uses the existing authenticated boundedBlob client for the complete admitted metadata list, limited to1MiB and1000 validated rows; overflow/malformed/failed input makes the whole type unavailable. It searches titles only and displays20 matches with exact total. The selected preview contains read-only title/status/category reference, not editor navigation or AI/content retrieval. Query limit256 excludes controls.

Every new query/type/route/identity invalidates and aborts prior work; token/session generation and tenant/actor/role identity are checked after both network and blob-text awaits. There is no persistent cross-query cache. Existing keyboard shortcut, native type selector, explicit Clear/Escape and preview close-focus restoration are retained. An identity-keyed session prevents old visible state; results sit in an anchored bounded scroll panel outside fixed header flow. Preview opening focuses its heading, closing returns to a connected opener. Synthetic tests cover full-list totals, type availability, view-preserving clear, query/type/identity races including delayed decoding, malformed/duplicate/oversized metadata and successful ticket navigation.

Validation:Initial full suite411 dashboard tests across57files passed. After coordinator review,37 focused search/Layout cases and types pass; final14-case search rerun verifies same-generation tenant/actor/role visible-state and decode cancellation plus preview focus. Final dashboard build passes. The full suite was not redundantly repeated; exact-head CI remains required. No new class-mirroring test, browser/native run, server change or ordinary-CI specialist job. Initial empty-status test ambiguity was corrected by rendering status only when needed. No dashboard lint script exists, so none is claimed.

Zero model/Copilot requests; deterministic component work reused root-verified source packet0d687c. Manual review triggers checked. Actual keyboard/screen-reader and tenant-negative integrated acceptance remain separate; Customers unavailable is an approved scope interpretation, not full #131 completion. Preserve Project progress/baseline/forecast pending defensible weighting; root retains review/merge.

Root actual browser found the historical /tickets redirect converted global query into persisted listQuery. The original route-only mock missed this. Corrected Tickets uses the existing admitted20-row/page1 response with validated exacttotal,1MiB cap and explicit Alltickets conversation links. Real mountedInbox regression proves current-view filter and workspacewrites unchanged; Customers/Knowledge contract unchanged. Final affected checks are recorded in PR.
