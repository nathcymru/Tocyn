# #48 current integration checkpoint

DraftPR167, codex/48-headless-primitives at/private/tmp/tocyn-beta2-48. Coordinator owns integration; latest allocation and evidence are recorded at the end of this file. Branch rebased cleanly on accepted79main12f4a5b.

Accepted79permissions page now consumes shared native buttons/inputs. Its aria-disabled save behavior, duplicate-save guard, failed-refresh recovery, checkbox semantics and styles are preserved. Shared input minimum sizing excludes checkbox/radio/hidden types so the custom label owns their target sizing rather than enlarging an invisible input. The three permissions workflow regressions and dashboard build passed; existing large-chunk warning remains. Full visual/target-size verification across checkbox consumers remains required, not inferred from DOM tests.

Remaining48scope: ALLbase-component/complex-control migration, interaction parity, measured bundle/startup/interaction comparison, actual browser/accessibility verification after shared composition. Two Arkcreate-ticket dialogs and native button/input/select/textarea migration are partial foundations. Do not close48or claim beta2acceptance. Forecast/authoritative coordinator-state refresh is PR171; prefer its current state after acceptance over older branch checkpoints. Existing retained-behavior matrix remains the source inventory.

Mobile navigation now uses the shared Ark dialog rather than a local native-dialog lifecycle. Label, close-button initial focus, Escape return to opener, route-selection focus to Workspace, aria-controls and edge layout are preserved by five layout interaction tests. Found and fixed shared-dialog custom-ID integration: the root content ID now matches the caller's content ID so Ark can resolve its focus target. JSDOM layout is explicitly supplied for focusable discovery; browser containment/visual verification remains separate. All76dashboard/59portal/6shared tests and dashboard build passed; existing bundle warning remains. Remaining complex menus/settings overlays and complete48browser/performance acceptance are still required.

Rebased onto accepted PR171main6299bfe. The only conflict was historical coordinator state; kept the new authoritative state and retained the earlier15:05receipt inside its historical section. PR171external publication verified and current top-level state updated. No application conflict or dropped acceptance.

Settings-dialog increment: user-details and filter editor now use shared Arkdialog with labels, initial focus, Escape/explicit-close behavior and opener restoration. Filters guard duplicate submissions and pending dismissal, disable editing during save, and retain the draft with an alert on failure/retry; condition fields/operators/values and remove controls are named. The user activity view previously displayed fixed demonstration events unrelated to the selected user; replaced those with an explicit no-records-loaded message. No user activity backend/profile editing/invitation feature is claimed or implemented. These absent capabilities must not be inferred from the preserved UI entrypoints.

Sixnew regression tests pass; full dashboard82tests/14files and build pass. Build exposed and corrected an unsupported Testing Library exact option; no application check was waived. Existing large-chunk warning remains. Actual browser/assistive-technology acceptance and remaining group/ticket-field/settings overlays/menus remain open; no48completion claim.

Ticket-field editor increment: shared named dialog, labelled controls/native Active checkbox, pending-save dismissal/duplicate guards, retained failed draft and retry. Both opener buttons are tracked so cancellation restores the actual trigger. Four regressions pass (full dashboard86tests/15files); dashboard TypeScript/build pass after correcting the mutable opener-ref type. Existing bundle warning remains. Safari on isolated5190 against synthetic fixture8899 verified Display Label initial focus and cancellation return to the empty-state opener. VoiceOver read the Display Label required/form context; navigation commands failed and no complete screen-reader pass is claimed. AppleScript control was observed off, temporarily enabled under standing authority, and a restore-off click was attempted; Computer Use then timed out repeatedly, so final setting read-back remains unverified and must be checked before the next session. No synthetic field was saved, no remote services used. Remaining48scope/forecast unchanged.

Group-members dialog increment: migrated the retained membership workflow to shared Arkdialog. Close is the initial focus target; Escape/Done restores the actual Members opener. Removal is visible without hover and requires an in-dialog, named-member confirmation with explicit focus; cancellation makes no request. Membership writes guard duplicates/overlapping actions and dismissal, retain search on failure, report fixed safe errors and support retry. Success announces status and moves focus off a potentially removed control. Existing admin-only controls and group/user payloads remain; unavailable membership data disables additions. Four regressions pass; full dashboard90tests/16files and TypeScript/build pass. Existing large bundle warning remains. This does not claim backend permission changes or complete browser/screen-reader acceptance. Group creation/deletion, other retained complex controls and remaining48performance/accessibility gates stay in the queue.

Administration-dialog increment: group creation/deletion and knowledge deletion now use the shared dialog. Group creation retains the existing cancelled draft, keeps failed drafts, guards pending duplicate submission/dismissal and restores its opener. Group deletion begins on Cancel, retains failed confirmation and moves success focus to the page heading because the original row may disappear. Knowledge category/article deletion preserves endpoint/target distinctions with equivalent guard/retry/focus behavior. Knowledge category actions no longer require hover; selection uses named pressed-state buttons. Seven additional regressions pass; full dashboard97tests/17files and TypeScript/build pass. Existing large bundle warning remains. These are UI regression tests, not new backend authorization evidence.

Current next48queue: remaining native confirmations/settings feedback (filter, automation, API-key, email/widget/security flows), retained menus/popovers and complete inventory parity, browser/assistive-technology/target-size/reduced-motion checks, and measured baseline/candidate performance budgets. Ticket-table composition is governed by replacement/compatibility classification and future128workspace ownership; do not reproduce obsolete product composition merely to migrate it. No48completion or forecast change is claimed. Earlier pending group/ticket-field/knowledge-overlay notes describe historical checkpoints, not the current implementation queue.

Shared confirmation increment: `TocynConfirmDialog` provides a named/described confirmation with Cancel initial focus, disabled pending actions, static token-based presentation and caller-owned mutation/recovery. Filter and automation deletion now consume it; both retain failed targets for retry, guard duplicate/pending dismissal, return to the opener on cancellation and a stable heading after success. Automation success/error feedback has live status/alert semantics. No automation execution/provider behavior changed.

Contract inspection also found and corrected missing content-ref forwarding in the shared dialog; both wrappers now expose their actual HTMLDivElement ref and preserve caller ARIA descriptions. Five new regressions cover confirmation behavior and native ref/event/description lifecycle, including downstream interface extension checked by TypeScript. Final dashboard101/portal59/widget3/shared7tests pass; UI typecheck and all3client builds pass. Existing bundle warning remains. Remaining confirmations/API-key/email/security/widget feedback, retained menus/controls and complete48browser/performance gates stay open. No forecast or acceptance percentage changed.

API-key UI increment: creation/revocation consume shared dialogs with labelled controls, guarded pending submission/dismissal, visible recovery and stable focus. Unknown creation outcomes explicitly remain uncertain and refresh metadata before the operator decides whether to retry. Revocation removes only an acknowledged target, ignores stale responses for that target, and clears a matching one-time key display. Clipboard feedback awaits success, handles failure with manual-copy/retry guidance and clears pending/timer state when the displayed key changes. List-load failure no longer claims an empty list or logs raw errors. Existing server permission revalidation/tenant write fence was inspected and is unchanged; these UI tests do not claim new backend authorization evidence. Seven regressions pass; full dashboard108tests/19files and TypeScript/build pass. Existing bundle warning remains.

VoiceOver restoration resolved10September2026 at17:56BST: after the owner completed the macOS password prompt, Computer Use successfully read VoiceOver Utility's “Allow VoiceOver to be controlled with AppleScript” checkbox as0(off). This supersedes the earlier unverified-restoration limitation. It is not a complete screen-reader navigation pass; that acceptance remains open.

Channel-controls increment: widget save now guards duplicate requests, disables changes while pending, always releases the guard after failure and retains user choices for retry. Background query updates do not overwrite unsaved choices; unavailable settings cannot silently write defaults. Save/copy feedback uses status/alert semantics and clipboard success waits for resolution. Checkbox labels provide explicit help associations and minimum-height label targets. The embed example now uses the widget's actual data-widget-key selector, labels its public-key/API-build/customer-sign-in prerequisites, and removes the ignored LUMINA_WIDGET_CONFIG example and unproven absolute CSS-isolation claim. This does not implement or accept67embedding/lifecycle/isolation; actual setup/hosting remains separate.

Email-channel removal now uses shared confirmation with initial/return focus, guarded pending mutation/dismissal and retry after an unconfirmed result. No provider activation, credential write, remote resource or external email was performed. Six new regressions pass; full dashboard114tests/21files and TypeScript/build pass. Existing bundle warning remains. Email add/provider settings and remaining security/profile/menu controls remain to inspect and migrate as required.

Allocation update: existing native evidence_copyedit worker successfully resumed for a bounded read-only remaining-control inventory while coordinator publishes this increment. Resume tool does not expose model/effort overrides or current model metadata; no model switch or separate allowance is claimed. Worker may not modify files/GitHub, recurse, request reviews or use remote resources. Earlier exhaustion applies to new worker creation and the known stopped permissions worker, not this verified resumed run. Coordinator remains sole writer/integration owner.

The resumed evidence_copyedit inventory is now complete, with eight concrete next areas recorded in ui-48-next-controls.md and current source locations checked by the coordinator. No active worker remains from that inventory; no overlapping writes occurred. This successful reuse does not remove the known new-worker creation limit or the permissions worker's execution limit.

Widget ticket-form increment: explicitly associated labels, read-only authenticated email, duplicate-submit guard, pending announcement/disabled editing, retained failed content with focused alert, and success/new-draft focus. The existing request URL, authenticated headers, credentials omission and body contract are unchanged. Two regressions bring widget coverage to 5 tests/2 files; widget build and new strict typecheck pass. Shared UI typecheck and workflow validator pass. Existing CI now includes widget and shared-UI typechecks without removing other checks. Dashboard114/portal59/shared7 results are prior unchanged-subsystem evidence, not reruns. Browser/assistive-technology, remaining controls and performance acceptance remain open; no #48 completion or forecast change.

Widget shell/chat increment: Ark Tabs is exported through the shared headless entrypoint and consumed with manual keyboard activation (arrows/Home/End move focus; Enter/Space selects). Named launcher/close controls expose expanded state; Escape restores the launcher. Hidden mounted panels preserve local drafts across tab/close/reopen; disappearing sessions unmount private content, and changed login email keys fresh content. Ticket-only configuration selects the enabled feature. AI chat has a named input/send action, conversation log/pending announcement, synchronous duplicate guard, retained failed question with focus and explicit retry, malformed-response handling and no fabricated assistant error message/raw logging. Scrolling is non-smooth and loading dots no longer bounce. Request credentials/history contracts remain unchanged; these mock tests are not backend authorization proof.

Validation: widget11/dashboard114/portal59/shared7 tests pass; widget/shared typechecks and all three client builds pass. Added six widget regressions cover keyboard/disclosure/draft continuity, AI-off mode, session disappearance, pending/history and HTTP/malformed-response recovery. Actual browser/shadow-root navigation remains a required check, not inferred from JSDOM; #67 still owns environment/embedding isolation. Ark automatic-selection timing did not pass the initial DOM test; final manual-activation semantics are explicit and tested, with actual-browser acceptance outstanding. Existing Vite/bundle warnings remain; measured full performance budgets and remaining retained controls remain open.

Layout disclosure increment: account and connection details now consume shared Ark Popover rather than document-level outside-click/Escape implementations. Named content, first-action focus, dismissal/trigger return and reconnect behavior are covered. Account navigation has an explicit Workspace final-focus target, fixing a rapid-navigation focus race; duplicate sign-out requests are guarded while failed server sign-out still clears local authentication and shows the existing warning. Seven layout regressions and full dashboard116tests/21files plus TypeScript/build pass. JSDOM supplies ResizeObserver/geometry; real-browser positioning, nested mobile navigation and assistive-technology acceptance remain required. Existing bundle warning remains. No authentication API/authority or realtime semantics changed; other clients/shared source unchanged in this increment.

Ticket-detail retained-control increment: QA actions are always visible, named pressed-state buttons with minimum-height targets and explicit light/dark button contrast on a white action surface. A synchronous pending guard prevents overlapping QA writes; fixed error/status feedback replaces raw-error alerts. Existing viewer names, including overflow beyond three avatars, are exposed as text to assistive technology; decorative avatars are hidden from its tree and the pulse is removed. QA marked replaces INDEXED because the service updates QA state before vector upsert, so a flag alone does not prove indexing.

Full dashboard118tests/21files and TypeScript/build pass; two new regressions cover pending/failure/retry/pressed state and filtered full viewer names. Browser/computed contrast/target-size acceptance remains open. Backend QA contract discrepancy discovered: current UI sends question/answer/null, while TenantKnowledgeService.markArticleAsQA declares answer/sop/null and the handler accepts unvalidated JSON. This predates the increment; backend QA validation/contract acceptance is unresolved, recorded here and in48receipt, and must be assigned through existing knowledge/QA scope before claiming that integration. No backend changes, real AI calls, provider activation, or full48completion.

Safari18:41BST10September: desktop popover/destination focus and390×844mobile focus wrap/nested Escape verified on synthetic5190/8899. Screenshot exposed account-content clipping; fixed positioning corrected it and was rechecked visually. Dashboard119tests/build pass. Evidence: docs/security/evidence/ui-48-safari-popovers-2026-09-10.md includes tested source blob/limits. Responsive mode restored. Disconnected realtime, full spoken VoiceOver and remaining performance/cross-browser acceptance stay explicit.

Performance baseline checkpoint: fresh accepted-tree baseline andfcba36fcandidate production builds measured by tools/ui-performance/bundle-evidence.mjs; repeat output identical excluding timestamp. JS gzip growth dashboard7.35%, portal24.92%, widget9.15% (Vite8.2.2both). JSONartifact and reproducible method/limits in docs/security/evidence/ui-48-performance.md. No numeric budget/startup/interaction acceptance inferred; investigate portal growth and build real-browser timing/actual Worker graph evidence next. Existing edge/rehearsal contract tests initially exposed a missing local better-sqlite3 native binding; rebuilt that installed dependency from source, then37contract/3runtime tests passed. No repository dependency/version change or check waiver. Read-only worker identified missing timing harness and completed. No48closure/forecast change.

Portal startup increment: authenticated ticket list/detail route modules now load on demand; login/verify and auth-generation fencing stay unchanged. Route loading has a status announcement; load failure focuses a safe message and offers a real document reload (React lazy rejection is cached), without mutating auth. Route changes reset the boundary. Portal61tests/build and lint pass; build outputs split initial/route/shared assets. Total bytes and initial static dependency closure will both be measured, not substituted for one another. Real-browser startup/interaction timing and numeric budgets remain open.

Clean-revision70a7a35manifest evidence: portal initial static JS gzip67502bytes versus baseline77808(−13.24%); all-route99830versus77808(+28.30%). Both recorded in ui-48-bundle-70a7a35.json, without changing immutable first snapshot. All-client manifest builds passed. Portal61tests/lint/build passed. Real-browser timing/CI budgets and full48acceptance still open; do not present code splitting as a reduction in total bundle bytes.


Security profile increment: shared confirmation uses Cancel initial focus and guarded pending dismissal; setup/confirmation prevent duplicate submits, preserve failed-code recovery, clear local provisioning data on session change and restore deliberate focus. Successful enrollment adopts the replacement token returned by the existing endpoint, retaining verified tenant context; stale responses cannot replace a newer session. Optional MFA disable signs out after success because migration0022 revokes the current token and the endpoint returns no replacement. Mandatory operator MFA remains enforced; no backend change or live MFA mutation was performed.

Validation: eight new synthetic regressions; full dashboard127tests/22files and TypeScript/production build pass. Existing bundle warning remains. A direct ESLint attempt could not run because dashboard/root have no ESLint configuration or dashboard lint script; no lint pass is claimed. Focused native worker inspection found no material regression; coordinator additionally checked the authority-change trigger and covered optional-disable logout. Full browser/spoken VoiceOver, performance budgets and remaining control acceptance stay open. No completion percentage, forecast, merge or release change. Owner reconfirmed local test-control authority; no further permission request pending.


Email settings increment: associated input labels and named forms; provider load/save errors are visible independently of add-channel UI, with explicit load retry. Dirty provider edits survive refetch; pending writes guard duplicate submissions and disable editing. Successful credential save replaces the local typed value with the existing mask, preserves masked-key omission, and announces configuration without claiming verified delivery. Add-channel errors preserve entries and focus recovery; pending cancel is disabled and successful/cancelled forms return focus to Add Email. Channel-load failures are distinct from an empty list. No provider/API contract change or real provider request.

Validation: four new mocked-request regressions and existing two removal-dialog tests pass; full dashboard131tests/23files and TypeScript/build pass. Existing bundle warning and absent dashboard lint configuration remain. Full browser/spoken/performance acceptance is pending. Native read-only worker corrected an initial QA ownership inference: #84 is AI-agent evaluation, not ticket knowledge curation; current #151/#152 do not explicitly assign the legacy marker mismatch. Owner clarification requested for explicit Answer/SOP alignment under152 with no silent conversion of existing Question records; independent48/159 work continues. No merge/closure/forecast change.


Browser timing harness added with installed bundled Playwright1.62.1/Chromium (no download/billing). Two-sample smoke run exercised production login startup/denial recovery for both baseline and candidate dashboard/portal with local fixed authentication denial and external-request blocking. Smoke is harness validation, not final timing evidence; fresh clean-revision builds and20-sample measurement follow. Quantile/bounded-input tests pass. No numeric budget or full48acceptance claim.


Clean3e08914real-browser evidence complete: freshly rebuilt baseline/candidate clients,80recorded Chromium login startup/recovery samples plus4warmups. Raw samples/artifact metadata and p50/p95/p99 in ui-48-browser-3e08914.json; summary in performance.md. No broad performance/budget acceptance inferred.

Owner approved Answer/SOP compatibility correction under152 on10September: UI and strict new/update validation match active service, SOP internal/publicAnsweronly, unsupported markers rejected, existingQuestionrecords preserved. Current152body now includes approval and acceptance; full151→152 dependency remains for larger delivery. Native evidence_copyedit resumes implementation of server-only correction/tests in isolated codex/152-qa-marker-contract worktree from accepted6299bfef. No model/effort control available on resume; coordinator owns48UI,GitHuband integration; zeroCopilot/no externalresources. Earlier pending-question/ownership-gap notes are superseded by this decision.


Approved152UI compatibility consumer: write controls now offer Answer/SOP with explicit internal-procedure naming. Read types retain legacyQuestion and includeSOP/null; legacyQuestion records display a compatibility notice and cannot be silently changed by these controls. A regression proves rendering does not send a conversion request; existing pending/retry test now verifiesSOP. Full dashboard132tests/23files, dashboardTypeScript/build and serverTypeScript pass. The wider152assistant scope remains incomplete. Server validation lives in isolated152worker branch and must integrate before48closure; pending runtime evidence is not supplied by these UI mocks. Synced current owner-approved152body into master-package issue source.


Dashboard route-loading increment: protected ticket/knowledge/settings pages load on demand; login,MFA,enforcedsecurity route and authentication guards remain eager. Each lazy page has announced loading and focused safe-error/document-reload recovery, matching established portal behavior; auth state is untouched.134dashboardtests/24files andTypeScript/build pass. Intentional failed-module regression logs a React/JSDOM error while passing; no test failure concealed. KnowledgeEditor remains a large deferred chunk, not removed functionality. Clean revision byte/timing evidence follows; no full startup/performance acceptance yet.


Clean a52904f browser artifact: dashboard initial JS gzip−74.1%, all-route+11.9%; local startup p95164.1→63.7ms, recovery48.7→49.9ms. Raw80samples/hashes/method in ui-48-browser-a52904f.json. Not a full performance/budget pass. Knowledge correction172mergedsigned4fce7b6654db924d818d2ee2ad77d3c855ee9d46 after exact9c46checks; reviewedtree528d88f49a34fc3c888274357937b1eb8d5644d1matches. Review-only ownerbypass used, no reviewfindings/Copilot. Refresh48from acceptedmain before furtherintegration; full152and48remainopen.

## Browser and Worker evidence — 10 September 19:53 BST

Real Chromium151 fixture passes default/radical/no-CSS keyboard interaction and failed-action recovery. Default select measured19px before correction; shared CSS now applies the target-height token to selects and textareas. Styled fixtures enforce44/48px minimum heights and reduced-motion1ms; noCSS asserts behavior only. Source-hashed receipt: docs/security/evidence/ui-48-controls-e0f5907.json. Full screen-reader/contrast/application/Combobox/Splitter acceptance remains open.

Actual Worker dry-build162inputs contains no React/Ark/Zag/client/agent/CSS-engine paths. Local Worker runtime passes on explicitly selected TCP8799 while preserving logical localhost8787 allowlist; default launcher remains8787. No application origin policy changed. Script TypeScript passes; launch test not rerun because user fixture owns8787. Receipt: docs/security/evidence/ui-48-worker-e0f5907.json. No remote resources touched. Forecast unchanged.

## Expanded complex-control proof — 10 September20:01BST

Chromium default/radical/noCSS fixture now also covers Combobox selection and Splitter keyboard resize/Home/End constraints. Two successful runs after fixing fixture focus-restoration timing; source-hashed final receipt ui-48-controls-d4cd040.json. Consumer panel definitions remain stable; combobox focus settles before moving keyboard focus. No application change in this increment. Screen-reader/contrast/fullapplication gates remain; earlier missing fixture Combobox/Splitter evidence is superseded by this receipt.

## VoiceOver ticket-field evidence —10September20:06BST

Owner reconfirmed access. Native spoken-output/cursor actions now work; Safari27/macOS27 on synthetic5190/8899 announced ticket-field dialog/form/required fields/type/Active/Cancel and returned to CreateField after cancellation. No data saved. Evidence ui-48-voiceover-ticket-fields-2026-09-10.md records exact source blob and limits. SystemEvents keystrokes still denied; no setting changes by agent. Owner-enabled VoiceOver remains enabled. Full AT/contrast/application acceptance remains open; do not repeat older blanket-unavailable claim.

## Filter/security spoken evidence —10September2026

NativeVoiceOver traversed filterrequiredname/form/emptyconditions; AddCondition revealed correctlynamedfield/operator/value/removecontrols. CancelreturnedCreateFilter; nosave. MandatoryMFAenabled/non-disableable textannouncedonSecurityProfile; noauthchange. Exactsourceblobs/limits in ui-48-voiceover-filter-security-2026-09-10.md. FullAT/contrast/applicationacceptance stillopen. RootcontinuesUI; actualWorkflowruntimeproof independent159agent. Noforecastchange.

## Rendered contrast correction —10September2026

Actual Chromium computed styles exposed three low-contrast default text pairs: Channels2.564:1, no-condition explanation2.451:1, MFA-enabled status3.296:1. Darkened slate400→500 for firsttwo and green600→700 for MFA status; browsermeasurements now4.759,4.548,5.016 respectively (assertions use unrounded4.5minimum). Labels/behaviorunchanged, CRLFpreserved. Reproducible loopbackonly check tools/ui-browser/contrast-check.mjs andsource-hashed receiptui-48-contrast-21cf229.json. Fullvisual/contrastscope remainsopen; noexistingbaseline rewritten or arbitraryperformancebudgetratified.

## Built widget browser correction — 10 September 20:40 BST

Root tested actual production IIFE in a disposable loopback Chromium page. Found undefined process global before launcher and missing ShadowRoot environment for Ark tab navigation. Build now explicitly replaces NODE_ENV per build/dev command; production React branches disappear without breaking Vitest development act. Existing legacy mount passes its actual shadow root through exported EnvironmentProvider. Removing that provider reproduces ArrowRight timeout; restored source passes.

Fresh widget build, 11 widget tests, widget/UI typechecks and real browser AI-on/off scenarios pass. Browser covers launcher/close focus, manual tab activation, cross-tab draft retention, failed submit/retry focus and new draft focus; backend is intercepted synthetic data, not auth/tenant proof. Vite widget gzip falls from159.06kB before correction to62.48kB; not a global performance-budget acceptance. No wrapper67 lifecycle/style-isolation implementation claimed. Full48 visual/AT/performance gates remain; draft167/open48 and forecast unchanged.

## Retained popup and inventory — 10 September 2026

Read-only Luna/medium audit identified incomplete granular inventory and ticket-table action overlay. Root verified current source, replaced its overlay/Escape implementation with shared Popover while preserving the compatibility View Ticket link, and added the retained composition inventory. Nested knowledge category buttons remain a disclosure/selection composition; no invented ARIA-tree behavior is claimed. Future primary workspace/table replacement stays #128.

All134dashboard tests and fresh TypeScript/build pass. Actual Chromium with one intercepted synthetic row verifies initial link focus, unchanged href, Escape focus return and outside dismissal. No saved data/provider action. Tests use ResizeObserver stub only in JSDOM; actual browser supplies the real API. Full48 acceptance remains open: downstream interface extension coverage, affected flow AT/visual evidence, justified numeric CI budgets and exact integrated revision. Widget receipt contains dirty-worktree source/artifact hashes of the corrected build; its parent revision is not falsely claimed as already containing the fixes.

Latest observed Codex allowance10%remaining, reset15September02:24BST; task usageunknown, Work/Codex countedonce. No Copilot or paid capacity. Two native workers this interval: Terra/medium local collector, Luna/medium UI acceptance audit; both complete. Coordinator integrated source and evidence.

## Named contract/ref closure — 10 September 2026

Native Luna/medium worker implemented workspace forwardRef wrappers and initial compile examples. Root expanded missing direct interface-extension cases to all13Tocyn-owned components, added explicit named ref fields, negative native/ref/state checks and runtime assertions for all5workspace refs/unmount cleanup. Shared7tests and TypeScript pass. No claim of every vendoredArkinternal part tested; upstream public types remain available. Compile-only fixtures are not application imports. Remaining48gate: affected-flowAT/visual/contrast, justified numeric performanceCI budgets and final integrated checks; no closure/forecast change.

## Numeric performance regression gate — 10 September 2026

Coordinator added explicit bundle and login-startup/recovery budgets plus deterministic receipt checks to required build CI. Uses immutable accepted6299baseline tree, own baseline lockfile, pinnedPlaywright1.62.1 and20alternating samples. Initial/all-route/CSS limits are separate; relativep95 and absolute limits both apply. Rationale and remaining scope are in ui-48-performance.md. No historical baseline changed and no production/workspace-wide SLO claimed. Playwright is development-only; locked package install adds3packages and reports0vulnerabilities. New browser dependency/runtime is for CI, not deployment or paid provider use. Final local/CI measurements still required before claiming these limits pass.

Clean857510c local performance run:80recorded alternating observations plus4warmups,16budgetchecks pass. Raw receipt ui-48-browser-857510c.json and evaluated limits ui-48-budgets-857510c.json. DashboardinitialJS125888/all543900; portalinitial67503/all99832; widget61723gzipbytes. Startup p95dashboard62.9/portal62.5ms; recovery48.4/64.8ms. Both dependency audits0vulnerabilities. Required new build CI still needs its own runner evidence; no full48closure.


## CI, email VoiceOver and widget timings — 10 September 21:36 BST

Exact19b9bbd CI passed, including16numeric performance checks. Preserved raw runner receipt34524932528 and evaluated limits under docs/security/evidence, retaining original tested merge revision. Owner-enabled VoiceOver/Safari passed email settings labels, opening and cancellation/focus return on synthetic5190/8899; no save/provider/mail action. Email source blob and limitations recorded in ui-48-voiceover-email-2026-09-10.md.

Built widget harness now supports bounded1–50samples, excludes warmups for repeated runs and records browser-clock startup/open/recovery raw quantiles. Fresh-build20samples/mode (40recorded+2warmups) passed retained interaction assertions; p95 AI-on/off startup66.3/64.6ms, opening39.4/41.8ms, failed-submit recovery48.2/48.9ms. No pre-fix baseline fabricated; it could not start. Root corrected harness to observe hidden/attribute changes, rather than only child additions. No application behavior changed in this increment.

Native auth_sli_159 Terra/high continues canonical durability diagnostics independently. Additional UI worker spawn/resume failed at agent-thread limit; root completed this UI work. No Copilot, paid capacity or redundant review. Work/Codex allowance counted once; current balance not re-polled. Broader affected-flow AT/visual/contrast, widget numeric timing limits, authenticated interaction costs and final integrated checks remain open; no closure or forecast change.

Widget numeric timing CI follow-through: required build will run the40sample scenario and enforce p95 startup1500/open100/recovery150ms perAI mode. Rationale and raw-data validation limits documented;8performance-verifier tests andworkflowvalidation pass. Gate requires clean tracked source, permits the separately checked-out untracked immutable CI baseline. New exact-revision local/CI proof remains pending, so the prior 'numeric limits remain open' note now means verification of these added limits.


## Concrete remaining UI acceptance — 10 September 21:47 BST

Clean cfa5df2widgetgate passes6local limits; requiredCIbuild34527748711also passes. Downloaded rawwidgetCIreceipt and independently re-evaluated it; login/bundle16checks also pass. BroaderCItest was still running at this observation. Permanent widgetCI/local raw receipts now retained. Root also verified22computed QA state/focus rows (unmarked/Answer/SOP/legacyQuestion, normal/hover,44pxtargets and keyboard-visible outlines), no writes; actualQAserveracceptance remains merged172. VoiceOvercomposerpublic/private toggle/description proof added; no message sent.

Native local_collection_159 Terra/medium now implements NEW authenticated dashboard/portal timing harness files only; root ownsUIevidence/integration. Its read-only checklist is useful but two stale statements were corrected:172alreadymerged andwidgetnumericgatealreadyadded. Do not ask owner to reapprove scenario selection merely because an earlier draft matrix used broad wording. Current actual remaining scenarios: ticketQA/viewer spoken states; filtersavefailure/draftretry; securityenrollment/sessionrecovery; portal authenticatedroute recovery; widgetspoken/visual states; finalcurrentWorkerboundary/build/browserchecks. #66themes/#67wrapper/#128workspace/full140gate remain distinct. No forecastchange, Copilot or newauthorityneeded.

Authenticatednavigation harness returned and rootreviewed: replaced broadFunctiontypes with realPlaywrighttypes, guardedpartialserverstartup cleanup, startsmeasurementatDOMclick, requiresdetailheading andtwoframes, provesAforeign404alongsideBowner200 andvalidsessions. Real20samples/client passed; p95dashboard310.6/portal55.0ms. Rawreceipt withsource/artifacthashes retained. This supersedes the first350msmeasurement that includedpreclickautomationwait. NativeTerra/medium workercomplete; root integrates. New requiredCIbuild exercisesharness+types; no newnumericauthenticatedSLO andfailedread/retryremainexplicit. Previouscfa5df2allrequiredCI/securitychecks nowpassed; newintegratedrevision stillrequireschecks.


## Navigation CI correction and spoken QA — 10 September 22:02 BST

Requiredbuild3266e34 failedonlyatnewauthenticatedheadingselector; allbundle/login/widgetlimits passed. CIrawTAP in /tmp/tocyn-ui-ci-34528916719 reports loaded-headingtimeout. Portalheadingincludesvisiblestatus, so subject-onlyexactmatchingwaswrong; currentharnessrequiresactualheadingcontainingthesubject, withURL/readiness preserved. Addedboundedtoken-freestatus/classdiagnostics insteadofweakeningtimeouts orauthguards.

Root freshly rebuiltbothclients and reranreal20samples/client plus2failure/retrycases successfully. Dashboard'sbuilt-inretryrequires2injected503s; portalrequires1. Bothuseactualretrycontrols, recoveredfixturedata/twoframes; portalreturnsfocus toconversationheading. Rawreceiptui-48-authenticated-recovery-3266e34.json retainsdirty/source/artifactmetadata. Earlierlocalmeasurementsdidnotestablishfreshbuildprovenance themselves; preferthisexplicitfresh-buildreceipt forcurrentbehavior. No timingthresholdraised.

VoiceOverread Answer/SOP selectedstate/internalprocedurelabels andlegacyQuestionexplanation/disabledcontrols onread-onlyloopbackproductionUIfixture. No writes; temporary5191serverstopped, Safari restored5190, VoiceOverleftenabled. Scopedreceiptadded; notfullfinalATattestation.

Wiki drift found: Roadmap-and-releases stillpresentedoriginalFebruaryforecast ascurrent. Source nowpreservesoriginalhistoryandlinksacceptedmainledger22January2027/21January2028, withdateddraft167/170snapshot. LiveWiki publication/readbackfollows. NativeTerra/mediumworker nowownsnewfilter-recoverybrowsercheck only; rootowns priorharnessintegration. Securityworker continuesAPI/widgetcredentialSLIs. No forecastrecalculation, Copilot orremoteprovideraction.


Filterrecovery browsercheck returnedfromTerra/medium: rootaddedstrictloopbackoriginvalidation andreranit successfully. Synthetic503draftretention/identicalretry/fake200focusreturnpass, no persistedrecords orprovideractions. Receiptui-48-filter-recovery-db6bc39.json. Sameworker nowownsNEWsecurity-recoverybrowserharness only.

WikiRoadmap-and-releases publishedfromrevieweddb6bc39source, liveWiki b642f79e40d1b37fb431b24633b7dc42e4b8cfe8 andlocalbytecomparisonmatched. Preservesoriginalforecastas history, displaysacceptedJanuaryforecast andexplicitdraft167/170progress. No architecture/readinessclaimchanged. CurrentnavigationfixCI stillneedsreadback; do notreuseoldergreenhead.


## Widget spoken checks and security recovery — 10 September 22:17 BST

Owner reconfirmed enabled VoiceOver/AppleScript. Root inspected the built widget through actual Safari/VoiceOver: launcher, open focus, named tabs/selected state, ticket form and required field names, close/focus return passed. Scoped artifact/source receipt ui-48-voiceover-widget-2026-09-10.md; no message/account/provider mutation. Temporary5191 server stopped, Safari restored5190, VoiceOver remains enabled.

CI533850e required build passes, including all numeric gates and actual two-tenant authenticated navigation/read-retry. Original runner receipt ui-48-authenticated-ci-34530326420.json preserves tested merge f47faeae and dirty flag (includes CI baseline checkout), not rewritten to the PR head. Full test job failed unchanged historical process-ownership test: timed out waiting for nested child PID disappearance. All25same-file tests pass locally. One failed-job retry is in progress; no timeout/assertion weakened and no claim original failure is resolved.

Terra/medium local_collection_159 browser evidence found real SecurityProfile success announcement/focus lost through session remount and MFA setup retry focus before input commit. Worker now owns bounded UI fixes/regressions, preserving session invalidation and cache clearing. Root reviews integration; fake UI auth must never reach any backend API. Terra/high auth_sli_159 independently implements remaining password/MFA/customer/realtime credential observation in159; one existing fixed summary per request, no new denominator/event budget.

Live Wiki b642f79 remote fetch/read-back of Roadmap-and-releases matches reviewed source byte-for-byte. No forecast/baseline/issue closure changed. Remaining widget visual and integrated acceptance still open. Zero Copilot requests or paid capacity.


Security recovery fixes returned and root reviewed: confirmation announcement attaches atomically to replacement session, remains nonpersisted/generation-scoped and clears on consumption or later authority/session changes. Existing auth/cache remount remains intact. MFA retry focus runs after input commit; confirmation focus follows layout reset. Full browser asserts all repaired states; all unmatched API requests blocked, strict loopback enforced. Raw receipt ui-48-security-recovery-533850e.json. Full dashboard136tests/24files and TypeScript pass; one initial unrelated Escape-focus test failure passed isolated and on the full rerun, retained as transient evidence. No dashboard lint configuration exists; no lint pass invented. Worker complete; root retains integration ownership. Final CI on next pushed revision remains required.


## Built widget correction — 10 September 22:38 BST

Terra/medium visual worker found real missing utility styles in the built shadow tree: global index.css import could not style shadow content. Compiled ?inline CSS now enters only the shadow tree; legacy optional shadow additions remain. Corrected44pxclose target, visible focus,320px/short viewport sizing and placeholder/footer contrast. Root reviewed screenshots and expanded harness to fail unsupported text colors or missing actual keyboard outlines, then reran6cases and5workflow tests successfully. Default placeholder4.83:1/footer7.56:1, keyboard reaches SendMessage inside320×480scrollable panel. Widget11tests/types/build and40functional timing samples pass; numeric checker requires clean commit, so clean receipt/CI still pending. No budgets raised.

RequiredCIbuild now runs the visual harness and retains JSON/screenshots. Actual Safari/VoiceOver recheck of the corrected artifact passed named controls/form/selected tabs/open-close focus; no submission. Server stopped, Safari restored, VoiceOver stays on. Permanent visual/spoken receipts retain parentec3eb3c/source/artifact metadata.

Fresh ec3eb3c local Worker dry build has162inputs and no forbidden UI/agent/style-engine imports; actual local runtime stop/restart/revocation-policy checks pass and dispose teststate on8799. Full ec3eb3c CI including tests passes. Earlier533850e failed-job retry was cancelled by that push; its original failure remains recorded, not relabelled a pass.

Workers complete; root owns integration. #159foundation6d3ce48 is ready for partial integration after finalCI, issue remains open. Refresh48fromacceptedmain before final validation/push. Remaining48acceptance: final retained-behavior coverage audit, viewer-presence spoken evidence not yet demonstrated, final integrated gate and any discovered material defects; #66themes/#67wrapper/#128workspace/#140fullSLA/ownership remain separate. SharedCodex5%remaining, reset15September02:24BST. Latesthumanoperationalupdate22:38BST; next22:08UTC/23:08BST. No Copilot/purchases or arbitrary progress/forecast changes.
