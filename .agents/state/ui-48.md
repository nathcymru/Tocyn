# #48 current integration checkpoint

DraftPR167, codex/48-headless-primitives at/private/tmp/tocyn-beta2-48. Coordinator owns implementation/integration; no active UI worker is claimed. Branch rebased cleanly on accepted79main12f4a5b.

Accepted79permissions page now consumes shared native buttons/inputs. Its aria-disabled save behavior, duplicate-save guard, failed-refresh recovery, checkbox semantics and styles are preserved. Shared input minimum sizing excludes checkbox/radio/hidden types so the custom label owns their target sizing rather than enlarging an invisible input. The three permissions workflow regressions and dashboard build passed; existing large-chunk warning remains. Full visual/target-size verification across checkbox consumers remains required, not inferred from DOM tests.

Remaining48scope: ALLbase-component/complex-control migration, interaction parity, measured bundle/startup/interaction comparison, actual browser/accessibility verification after shared composition. Two Arkcreate-ticket dialogs and native button/input/select/textarea migration are partial foundations. Do not close48or claim beta2acceptance. Forecast/authoritative coordinator-state refresh is PR171; prefer its current state after acceptance over older branch checkpoints. Existing retained-behavior matrix remains the source inventory.

Mobile navigation now uses the shared Ark dialog rather than a local native-dialog lifecycle. Label, close-button initial focus, Escape return to opener, route-selection focus to Workspace, aria-controls and edge layout are preserved by five layout interaction tests. Found and fixed shared-dialog custom-ID integration: the root content ID now matches the caller's content ID so Ark can resolve its focus target. JSDOM layout is explicitly supplied for focusable discovery; browser containment/visual verification remains separate. All76dashboard/59portal/6shared tests and dashboard build passed; existing bundle warning remains. Remaining complex menus/settings overlays and complete48browser/performance acceptance are still required.

Rebased onto accepted PR171main6299bfe. The only conflict was historical coordinator state; kept the new authoritative state and retained the earlier15:05receipt inside its historical section. PR171external publication verified and current top-level state updated. No application conflict or dropped acceptance.

Settings-dialog increment: user-details and filter editor now use shared Arkdialog with labels, initial focus, Escape/explicit-close behavior and opener restoration. Filters guard duplicate submissions and pending dismissal, disable editing during save, and retain the draft with an alert on failure/retry; condition fields/operators/values and remove controls are named. The user activity view previously displayed fixed demonstration events unrelated to the selected user; replaced those with an explicit no-records-loaded message. No user activity backend/profile editing/invitation feature is claimed or implemented. These absent capabilities must not be inferred from the preserved UI entrypoints.

Sixnew regression tests pass; full dashboard82tests/14files and build pass. Build exposed and corrected an unsupported Testing Library exact option; no application check was waived. Existing large-chunk warning remains. Actual browser/assistive-technology acceptance and remaining group/ticket-field/settings overlays/menus remain open; no48completion claim.

Ticket-field editor increment: shared named dialog, labelled controls/native Active checkbox, pending-save dismissal/duplicate guards, retained failed draft and retry. Both opener buttons are tracked so cancellation restores the actual trigger. Four regressions pass (full dashboard86tests/15files); dashboard TypeScript/build pass after correcting the mutable opener-ref type. Existing bundle warning remains. Safari on isolated5190 against synthetic fixture8899 verified Display Label initial focus and cancellation return to the empty-state opener. VoiceOver read the Display Label required/form context; navigation commands failed and no complete screen-reader pass is claimed. AppleScript control was observed off, temporarily enabled under standing authority, and a restore-off click was attempted; Computer Use then timed out repeatedly, so final setting read-back remains unverified and must be checked before the next session. No synthetic field was saved, no remote services used. Remaining48scope/forecast unchanged.

Group-members dialog increment: migrated the retained membership workflow to shared Arkdialog. Close is the initial focus target; Escape/Done restores the actual Members opener. Removal is visible without hover and requires an in-dialog, named-member confirmation with explicit focus; cancellation makes no request. Membership writes guard duplicates/overlapping actions and dismissal, retain search on failure, report fixed safe errors and support retry. Success announces status and moves focus off a potentially removed control. Existing admin-only controls and group/user payloads remain; unavailable membership data disables additions. Four regressions pass; full dashboard90tests/16files and TypeScript/build pass. Existing large bundle warning remains. This does not claim backend permission changes or complete browser/screen-reader acceptance. Group creation/deletion, other retained complex controls and remaining48performance/accessibility gates stay in the queue.

Administration-dialog increment: group creation/deletion and knowledge deletion now use the shared dialog. Group creation retains the existing cancelled draft, keeps failed drafts, guards pending duplicate submission/dismissal and restores its opener. Group deletion begins on Cancel, retains failed confirmation and moves success focus to the page heading because the original row may disappear. Knowledge category/article deletion preserves endpoint/target distinctions with equivalent guard/retry/focus behavior. Knowledge category actions no longer require hover; selection uses named pressed-state buttons. Seven additional regressions pass; full dashboard97tests/17files and TypeScript/build pass. Existing large bundle warning remains. These are UI regression tests, not new backend authorization evidence.

Current next48queue: remaining native confirmations/settings feedback (filter, automation, API-key, email/widget/security flows), retained menus/popovers and complete inventory parity, browser/assistive-technology/target-size/reduced-motion checks, and measured baseline/candidate performance budgets. Ticket-table composition is governed by replacement/compatibility classification and future128workspace ownership; do not reproduce obsolete product composition merely to migrate it. No48completion or forecast change is claimed. Earlier pending group/ticket-field/knowledge-overlay notes describe historical checkpoints, not the current implementation queue.

Shared confirmation increment: `TocynConfirmDialog` provides a named/described confirmation with Cancel initial focus, disabled pending actions, static token-based presentation and caller-owned mutation/recovery. Filter and automation deletion now consume it; both retain failed targets for retry, guard duplicate/pending dismissal, return to the opener on cancellation and a stable heading after success. Automation success/error feedback has live status/alert semantics. No automation execution/provider behavior changed.

Contract inspection also found and corrected missing content-ref forwarding in the shared dialog; both wrappers now expose their actual HTMLDivElement ref and preserve caller ARIA descriptions. Five new regressions cover confirmation behavior and native ref/event/description lifecycle, including downstream interface extension checked by TypeScript. Final dashboard101/portal59/widget3/shared7tests pass; UI typecheck and all3client builds pass. Existing bundle warning remains. Remaining confirmations/API-key/email/security/widget feedback, retained menus/controls and complete48browser/performance gates stay open. No forecast or acceptance percentage changed.
