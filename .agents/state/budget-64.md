# #64 budget coordinator foundation — 11 September 2026

The accepted pure tenant transition model remains at `apps/server/src/budgets/coordinator-state.ts`. It provides JSON-serializable, all-or-nothing generic `ResourceAmounts` grants; holder binding; idempotency-envelope conflicts; separate new-work and recovery pools; revision fencing; expiry/lost-grant uncertainty; terminal-evidence reconciliation; and policy/restriction replacement. Grants end at the earliest selected interval boundary or policy maximum lifetime. `ResourceAmounts` are non-negative safe-integer units. The model retains expired, lost, revoked, uncertain and consumed charges; expiry never returns capacity or a record slot. A measured overrun is retained as actual accounted usage, records a defect and blocks admission rather than being clamped.

This slice adds `BudgetCoordinatorDO` and `owner-aggregate.ts`. A coordinator contains up to 128 server-derived tenant allocations that share one owner allocation set, and retains at most 4,096 grant records in total. Its one Durable Object state is the atomic persistence boundary for tenant and owner reservation/reconciliation transitions: every owner dimension, window and purpose is charged across every participating tenant before a new grant is committed. The coordinator rejects a tenant allocation that does not derive from the configured owner policy or tries to raise its limit, change its allocation/window, or change the recovery split. It retains a global capacity defect if any tenant reports an overrun, so it blocks unsafe admission across that shared owner allocation. Its internal reservation namespaces are server-issued and unique; they are never request fields.

`BudgetAuthorityRepository` now loads deployment-owned policy and tenant-allocation rows from D1. It first checks the supplied `VerifiedTenantScope` against the current `users` tenant, role and session-version row. Its one bounded D1 batch returns at most 129 allocations and revalidates the same deployment authority revision, owner policy revision, coordinator, lease value and allocation count, so it cannot assemble an aggregate from different policies or revisions. The lease is the deployment-owned `authority_max_age_ms`, not a universal 30-second value; it must be a positive safe integer no greater than that owner policy's `maxGrantLifetimeMs`. New grants end at the earlier of policy/window and this configured authority lease.

`BudgetCoordinatorService` is a non-active integration contract only: it requires a future handler to supply a current-authorization gate that rechecks credential expiry/session and capability before budget admission, then derives authority fields and the Durable Object name itself. It has no route or client-selected tenant/policy/address parameter. A D1 authority revision transition preserves every prior charge and marks outstanding grants uncertain before it applies the new active owner/tenant limits; historical allocations remain available only to reconciliation. A D1 revocation freezes admission without returning any accepted charge. A matching policy/restriction revision does not prove freshness: the coordinator rejects new admission at lease expiry until a coherent fresh D1 read renews the unchanged snapshot.

`BudgetGrantHolderDO` provides the durable warm path. After a committed grant is seeded by the server-only service, a server-derived holder object persists an operation receipt and decrements its remaining envelope before reporting a spend. Exact operation replays are idempotent, conflicting replays reject, concurrent over-spend rejects, and the holder refuses work after the grant/authority lease. It never calls the central coordinator for a warm spend. The current service is intentionally not wired into an application path pending root review of the authorization and envelope contract.

The D1 tables are deployment-owned storage only; this slice provides no tenant-facing writer, deployment-owner administration, owner actor audit record, endpoint, or production data seed. An approved #64/#90 follow-up must add that separately verified authoring path, policy-transition handling for outstanding grants, reservation-holder warm-path state, active admission envelopes, usage observation, durable terminal evidence, reset integration and production authority operations. It must not duplicate a full account ceiling into independent tenant coordinators. NotificationDO remains realtime-only.

`wrangler.json`, `wrangler.local.json`, and the observability-evidence local config add `BUDGET_COORDINATOR_DO` and `BUDGET_GRANT_HOLDER_DO` with SQLite Durable Object migration tags `v2-budget-coordinator` and `v3-budget-grant-holder`. D1 migration `0034_budget_authority.sql` adds the deployment authority, owner policy and tenant allocation records; `0033` remains reserved for #73. `apps/server/scripts/budget-coordinator-do-runtime.test.ts` runs real local Miniflare RPCs: simultaneous 50-unit requests from two 80-unit tenant allocations receive only one grant against the shared 80-unit owner new-work partition, then trusted reconciliation from 50 to 30 allows one later 50-unit grant for an exact aggregate charge of 80. `apps/server/scripts/budget-authority-runtime.test.ts` applies all migrations to local D1 and proves two tenant scopes derive one shared coordinator, warm spend is seeded to a holder, lease expiry requires a fresh D1 read, a lower revision transition keeps earlier charges uncertain, revocation freezes the DO, and session-version loss fails closed. `apps/server/scripts/budget-grant-holder-runtime.test.ts` proves persisted-before-reply warm decrement, concurrent over-spend rejection, idempotent/conflicting replay behavior and lease expiry.

Current bounded active-admission increment (uncommitted): the only wired budget boundary is authenticated API-key `POST /api/v1/tickets` and `POST /api/v1/tickets/:id/articles`. After the existing API-key permission/current-key checks and mutation replay detection, it reserves a fixed server-owned canonical-mutation/audit/authority/DO envelope before calling the D1 mutation commit. Replayed requests return their durable receipt without a second reservation. This does not wire `PATCH`, dashboard, customer, widget, email, staff/session, API-key management, attachments, provider, AI, notification, or background work; those flows need independently approved envelopes and credential-specific gates.

`BUDGET_ADMISSION_POLICY` is a server deployment setting, never request input. `api-ticket-mutations-v1` enables this boundary; explicit `off` keeps it off while deployment-owned authority is not configured; absent or malformed settings, missing bindings, a revoked/inactive API key, a missing policy/allocation, stale/revoked authority, or a coordinator fault return a fail-closed 503 before the mutation commit. Checked-in local/production/evidence configurations explicitly set `off`; no remote configuration was changed. Enabled quota exhaustion returns 429. The API-key gate rechecks the live active key and `tickets:write` permission and the repository repeats that check before loading the coherent owner/tenant authority snapshot. Session callers must return a current session principal; a matching `VerifiedTenantScope` revision does not establish credential freshness.

`apps/server/scripts/budget-admission-runtime.test.ts` is the real local Miniflare route proof: it applies all local migrations, seeds synthetic server-owned authority and an active synthetic API key, enables only the configured API policy, accepts one create, then rejects a second create and a reply at 429 and verifies that neither rejected mutation committed. Existing local-beta guard coverage remains unchanged and the global guard still precedes all routes. Remaining #64 work includes the listed non-API paths, measured/provider envelopes, reconciliation terminal evidence and recovery, warm-holder execution/replay integration, reset handling, owner authoring/audit, fresh authority operations, and production/staging evidence. This increment must not be represented as a complete application-wide quota or owner-account safety guarantee.

Node local-runtime boundary correction (uncommitted): `src/local-app.ts` now contains Node-importable local application composition, while the Wrangler `src/local-index.ts` retains its real `NotificationDO`, `BudgetCoordinatorDO`, and `BudgetGrantHolderDO` exports. Node fixture/workflow consumers import the app composition directly, so they do not need an unsafe Durable Object stub. The two-tenant fixture explicitly declares the server policy `off`; `npm run test:local-beta --workspace=apps/server` passes all 12 real-Miniflare local-beta tests after the split.

Material accounting correction (uncommitted): active API envelopes now reserve two Worker attempts, 3,072 bounded D1 row reads, 64 D1 row writes, and six DO requests/writes. The D1 read figure is derived from two 1,024-row indexed authority-snapshot allowances (initial admission plus a retry after a lost holder-seed acknowledgement), plus 1,024 fixed margin for API-key/current-receipt/canonical-mutation/replay reads. The authority snapshot has no SQL sort or unbounded count: each of its two coherent batch statements uses the existing allocation index and a 129-row sentinel; the process rejects the 129th allocation. A real local `EXPLAIN QUERY PLAN` regression proves that index at the 128-allocation boundary.

Authority JSON is capped at 64 KiB per persisted policy/restriction record and serialized coordinator snapshots at 96 KiB before any DO RPC. A synthetic five-dimension, 128-allocation configuration exceeds that active RPC bound and returns 503 before coordinator/holder work or a ticket write; this is intentionally fail-closed rather than pretending all schema-valid 128-tenant snapshots fit a Worker RPC. The successful active-route proof uses 32 allocations below this payload bound. The fixed 128 allocation count remains the database/coordinator structural ceiling, not a promise that every maximum-sized policy payload is admissible.

If a coordinator reservation commits but its holder seed fails, exactly one request retry with the same idempotency key sees the durable idempotent grant and may seed its holder before committing the application mutation; later delivery is rejected and the charge remains held. Warm spend now always rechecks current credential and coherent D1 authority, refreshes/revokes the owner coordinator, and asks the holder to fence owner-policy/restriction revisions before its local decrement. A lease alone is no longer considered evidence that revocation or policy change has not happened.

Finite retry/accounting correction (uncommitted): a coordinator grant persists `holderSeedAttempts` before every holder RPC. It authorizes only the initial seed and one crash-recovery seed; a third identical delivery returns `delivery-exhausted`, does not call the holder, and retains the original charge for terminal reconciliation. This bounds the only accepted same-idempotency recovery work and cannot create a second canonical mutation. Post-exhaustion HTTP attempts still pass the ordinary authentication/request-control boundary but are rejected admission attempts, not unbounded admitted work; this partial slice does not claim to budget arbitrary hostile request traffic.

Warm spend now pre-reserves two bounded control-plane attempts before its first holder decrement: each is one current credential/coherent-authority lookup allowance (1,536 D1 rows), coordinator refresh, holder freshness check, and holder write (three DO requests/writes). The durable holder permits one lost-acknowledgement replay and then returns `replay-exhausted`; the real local authority proof verifies the first spend holds both allowances, its one replay does not repeat business work, and the third is rejected. This is still a partial #64 foundation: no active warm-path caller is wired yet.

## Coordinator recovery correction — 11 September 2026

Root reviewed and corrected holder identity: versioned server-derived tenant/holder/reservation tuples prevent one API key's distinct grants from colliding. Reseeding compares immutable grant fields, preserves spent balances/receipts, and accepts only monotonic current authority with expiry bounded by both grant and authority. Admission rejects terminal/expired grants. Consumed holders can acknowledge an exact existing receipt once without admitting new work.

Independent Terra/high regression tests passed: real active runtime three scenarios (including two creates and two replies under one key, canonical replay and separate holders), authority runtime two scenarios including lost acknowledgement after persistence and expired retry, holder consumed-replay runtime, server types and diff hygiene. Full #64 remains incomplete. Current DO-backed warm adapter does not satisfy the final no-per-request-DO requirement; a separate Astra/high worker is implementing a bounded prepaid isolate-local grant module. This preserves the requirement rather than relabelling the existing RPC path as complete.

## Warm-path integration — 11 September 2026, 05:46 BST

Astra/high implemented actual isolate-local prepaid admission for API creates and exact-target replies: zero warm DO RPCs, current credential/coherent policy checks, bounded64scope registry, four blocks of up to8 operations, two cold retries and one bounded smaller fallback. Canonical attempt owns stable operation/payload intent; lost/expired grants remain centrally charged. No simultaneous durable-holder seeding. Twelve real Miniflare admission tests and two authority tests,83 focused unit tests, server/runtime types and ESLint passed. Other application paths and automatic recovery of incomplete canonical business work remain outstanding; this does not close#64.

CI remediation: SQL moved into the tenant repository/factory rather than lint exemptions. Node D1 integration imports have a fail-on-instantiation DO shim and explicit budget-off fixture configuration, preserving the separate enabled-runtime tests; complete Batch1–5 D1 integration now passes. API runtime fixture credentials now use fresh256-bit random tokens and the same Web Crypto digest protocol as ApiAuthResolver, replacing predictable literals; no production algorithm or CodeQL suppression changed. New exact-revision CodeQL/CI must pass before merge. Zero Copilot.

## Session admission prerequisite — 11 September 2026

SessionBudgetAdmissionService and factory-created SessionBudgetAuthorityRepository now provide fresh actor/role/session/MFA/expiry, current ticket/group and optional existing capability-fingerprint checks. The scope and policy reads are finite; no new permission catalogue or route behavior is invented.21combined API/session actual runtime tests,70focused tests, server/runtime types and full server ESLint pass. This is an adapter prerequisite, not wired dashboard acceptance.

Required before full dashboard admission: staff/dashboard canonical receipt variants (never mislabelstaff asAPIkeys), bounded NotificationDO accepted fanout, bounded sender selection, bounded outbound attachment rereads and full-operation request/resource envelopes. No canonical-only substitute or silentlydisabledrequired sideeffects is authorized. Notification bounds are next; article/draftformat0035 is owned in#68, potential staffreceipt0036 remains pending integration contract.

Observability fixture CI was updated to verify the exact three local DO bindings, explicit local budget-off setting and realWorkersbuilt-in module support. Configuration4/4, localcollector1/1 and operationalperformance5/5 pass; exactCI revision still required.
