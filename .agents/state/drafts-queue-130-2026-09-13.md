# #130 Drafts queue continuation

Branch codex/130-drafts-queue, based on signed main `3e61f4893efd4c53f861053af5234b8949afa4bd`. Coordinator approved
same-tenant/current-operator draft predicate, preserved live group visibility,
local-only expiry and additive4*ticketRows admission allowance before code.
Native GPT execution agent owns implementation/tests; coordinator retains final
auth/resource/release acceptance. Known Graphify packet was source-verified;
no independent graph crawl, nested workers, credentials or customer data used.

Implemented actual Drafts API/predicate, admission accounting and UI view. No
waiting engine, schema or production retention changes. Evidence and commands:
`docs/security/evidence/drafts-queue-130-2026-09-13.md`.

Checks:3native queue tests,5native list-admission tests,11focused dashboard tests;
server/dashboard/dedicated-script types; npm audits0. Initial query-plan assertion
was corrected to accept SQLite's EXISTS wording while still requiring the exact
primary-key index. Native-script implicit-any callbacks were annotated. The old
Snoozed UI test now waits for its authoritative response before inclusion text,
matching the corrected placeholder behavior.

Next: coordinator substantive review, GitHub-signed issue PR, exact-head required
CI/CodeQL, integration and progress receipt. Full #130 remains open. Original
2–4hour slice estimate covered implementation/native verification; remaining
release dependencies and assistive acceptance are not a Beta.2 ETA.

Final coordinator-approved correction: a deferred empty-Drafts to Snoozed test
reproduced premature queue-empty text. Both inclusion and empty claims now wait
for authoritative non-placeholder data, preserving confirmed rows/Refreshing.

Coordinator review also caught the table presentation of the same empty-placeholder defect. The deferred regression reproduced it in Table view, then passed with both list/table empty claims suppressed until authoritative data.
