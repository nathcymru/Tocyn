# #317 priority matrix checkpoint — 2026-09-20

- Owner: GPT-6 coordinator. Branch: `codex/317-priority-matrix`, stacked on the current #315 integration head. Issue: #317. This is a pure shared calculation and comparison batch; it does not change stored tickets, the live ring, or accepted calendar-aware response/resolution SLA clocks.
- A bounded GPT test task supplied the Vitest cases and module export. The coordinator checked the source and reran the focused test and server typecheck. Graphify authentication returned `oauth_token_invalid_grant`; no retry or source from another workspace was used. Local source and existing tests supplied the context.
- Category and scope tables, explicit modifier-total arithmetic, strictest absolute window, signed remaining time, and the three requested raw-tier sort chains are implemented. The comparator uses stable ticket ID only after all requested keys tie. Invalid runtime values fail explicitly. Conditions that produce modifier totals, tier assignment, artificial drift, status handling, snapshot authority and integration remain pending product/architecture decisions.
- Validation before this checkpoint: `npm exec -- vitest run apps/server/src/domain/__tests__/priority-matrix.test.ts` (9/9), `npm run typecheck --workspace=apps/server` (pass), and `git diff --check` (pass). The focused test runs in ordinary server Vitest discovery.
- Next: settle ring label and supplemental-vs-replacement clock questions with the maintainer; define drift, tier authority, urgency stacking, pause/reopen and legacy fallback behavior; then implement tenant-safe storage/API, whole-queue sorting before pagination, review fixtures and browser evidence. Keep #315's visual migration PR separate and the unrelated #243 worktree untouched.

## Threshold follow-up

- Added a pure urgency-window classifier: a 48-hour ticket reaches the 24-hour classification at exactly 24 hours remaining and the 4-hour classification at exactly 4 hours remaining, as the maintainer's examples show. It does not rewrite contract/criticality fields or alter accepted SLA deadlines. How this classification participates in each named view's tier sort still requires a product decision.

## Owner decisions and local review fixture follow-up

- The maintainer chose an A4-style ring plus a separate fixed-hour countdown alongside contractual calendar-aware clocks; operator-assigned criticality; three independently stackable +5 conditions; inclusive drift that changes sorting only; and ingestion-start elapsed time that pauses during waiting/resolution and resumes with debt after reopening. Default Focus uses descending score when exact remaining times tie. The pure shared module now covers those rules and rejects invalid values; focused tests 12/12 and server typecheck pass.
- Migration 0079 adds nullable, all-or-none category/scope/flags/score/contract/criticality columns. Historical rows are preserved without invented values; a DB trigger verifies score arithmetic and prevents a classified record from being cleared. Staff create can pass validated complete classification through both canonical and ordinary insert paths. The existing v1 ticket receipt projection is deliberately unchanged; adding fields there would silently change its frozen response contract. Strict classification of every new intake, audited operator reclassification and fixed-hour lifecycle storage remain open.
- The disposable `--local-beta` fixture was replaced after the maintainer confirmed it contains only synthetic seed data. Its new bootstrap classifies 20 tenant-A and two tenant-B tickets, covering 16 tier×level pairs, all 11 categories, two A4 overdues and two D1 drift examples. The running API on 127.0.0.1:8787 and dashboard on 5173 returned HTTP 200; a fresh synthetic operator password+MFA login returned 20/20 classified tenant-A tickets and two A4 overdues by raw age. The live ring is not yet connected to these columns.
- Coordinator verification on Node 22.19.0: migration/handler/classification Vitest 49/49; disposable fixture suite 6/6; ticket replay runtime 14/14; staff mutation runtime 42/42; server typecheck; `git diff --check`. These checks do not prove whole-queue ordering, live clock lifecycle, or browser ring appearance.
- The owner additionally requested Cloudflare Queue → first-line AI classification and attempted resolution for portal/widget/API/email, with validated safe defaults and human-review fallback. Current code has no Queue binding or consumer and the relevant #51/#76/#77/#82/#91/#243 dependencies are open. A question is pending whether the owner intends to supersede #77's operator-acceptance gate for customer-facing AI messages. No Queue, provider, automatic reply, or remote resource was enabled in this batch.

## 20 September integration checkpoint

- Integration owner: GPT-6 on `codex/317-priority-matrix`, draft PR #318, stacked on #315 / PR #316. The unrelated #243 branch and its dirty files remain untouched. #315 changes through `78aecf52` were merged at `bf746e24`; the #317 live-feed test race was fixed at `ede0eaab` before that merge. Current local preview uses a fresh run-owned D1/R2 fixture on ports 8787/5173. Direct D1 verification found exactly 20 `beta2-*` tenant-A tickets and two isolated tenant-B tickets, with zero unclassified rows in either tenant. No earlier demo records were carried forward.
- Coordinator verified the combined dashboard checkpoint with seven focused test files, 79/79 tests passing on Node 22.19.0. The previous exact-head #317 CI at `732b55ff` failed only two live-feed count assertions; the full local dashboard suite passed 589/589 after the corrected wait. The new combined head still needs dispatched exact-head CI and CodeQL once its remaining changes are ready. #315 CI passed at `92a8da84`; `78aecf52` was still running at this checkpoint.
- Three bounded GPT lanes are active: #317 server-side audited operator reclassification (server/migration/tests only), #317 Park detail editing UI (dashboard/tests only), and #315 parent Settings menu portal/keyboard geometry. The coordinator owns contracts, integration, checks, branch merges and acceptance. Ollama Local's last bounded proposal invented unsupported fields and was rejected; Cloud's earlier #317 proposal contradicted the approved lifecycle, so neither is counted as verified work. Graphify currently requires reauthentication; local source and focused index evidence are used.
- Remaining #317 gap: new staff tickets are fully classified, but existing tickets have no correction route. The approved correction contract is a classification-only staff PATCH with a full validated payload, server-owned score, additive integer classification revision and tenant-scoped audit, atomic compare-and-swap with a 409 conflict, and preserved accrued fixed-hour clock. External portal/widget/API/email paths still create unclassified tickets today; their proposed Queue/AI lifecycle awaits reconciliation with #76/#77/#82 before implementation. The current local review state is disposable and already fully classified.
- Browser evidence so far covers the 20-ticket populated inbox, A4 rings, eight overdue clocks and banner, hover snippet replacement, three sort views, light/dark reflow at 720 CSS pixels, reduced-motion pulse suppression, focus ring, and authorised/denied attachment downloads. Actual native 200% zoom and VoiceOver remain open because the Mac was locked during computer-use access. No remote deployment, provider enablement, production data change or release tag occurred.

## 20 September correction checkpoint

- The operator-only classification correction now has a strict complete payload, required idempotency key, additive optimistic revision, tenant-scoped audit, and server-computed score. Its atomic CAS preserves the existing fixed-hour clock and accrued debt; identical saves produce a receipt without revision, audit, clock or local-beta capacity changes. API/v1 mutation snapshots remain frozen. When staff mutation admission is not configured the correction fails closed with 503; this is a local-beta partial capability, not a production ingress claim.
- The dashboard uses a Park dialog with explicit conflict reload/review, retained choices, stable uncertain-retry key, identity-fenced invalidation and unchanged-save prevention. A save restarts active priority or calendar-SLA cursor sorting at page one without rereading stale pages and retains the selected conversation and reply draft.
- Coordinator verification on Node 22.19.0: dashboard 601/601, server Vitest 1006/1006, staff mutation real-D1 runtime 49/49, replay runtime 14/14, dashboard build, Park guard 12/12 plus 105 active files, and `git diff --check` passed. Portal 91/91, widget 19/19 and UI 41/41 tests, and widget build passed. An isolated synthetic browser at 320 CSS pixels showed dialog fit, scroll, keyboard containment and no page error; that is not literal native 200% zoom or VoiceOver evidence.
- Portal production build currently fails at four shared TypeScript errors in `packages/shared/ui-theme.ts` and the official Park Select/Combobox root casts. The #315 parent branch owns that correction. #130 snooze work is not merged yet; its local specialist launcher needs complete classification for two due-review rows after integration. Exact-head #317 CI and live correction browser checks remain open. The Mac is locked for Safari/VoiceOver acceptance. No remote deployment, production data or tag was changed.
