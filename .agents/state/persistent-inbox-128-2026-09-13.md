# #128 twenty-transition continuation

Branch `codex/128-twenty-transition-evidence`, based on signed main
`b84453fe`. Coordinator approved a bounded
acceptance gap after source audit: an existing test rendered20 fixtures but
selected only two, and mobile browser evidence used the default all view.

Native GPT execution agent owns this test/evidence slice. Reused coordinator's
Graphify source packet and verified known source directly. No nested delegation,
Ollama requests or browser/Terminal interaction. User's existing preview and
synthetic browser fixture remain untouched.

Added two tests in PersistentInbox.test.tsx for20 actual route selections with
custom filter/page/query/sort and retained pane state, plus a refused then
acknowledged draft-guard Back navigation. No application changes. All10 focused
tests, dashboard TypeScript and diff checks pass. Evidence and exact limitations:
`docs/security/evidence/persistent-inbox-128-2026-09-13.md`.

This30–60minute estimated test/evidence slice is implemented; coordinator review,
signed PR and exact-head required CI precede integration. Full #128 remains open
with #130 and real integrated/mobile/assistive acceptance; no new completion
forecast or arbitrary progress percentage is inferred. Preserve original dates.

CI initially timed out the20-transition case at5seconds. Reused stable DOM
references and gave only that case15seconds; all10 focused tests passed afterward.
The full dashboard rerun passed317/318, with an unrelated pre-existing activity
keyboard test failure. A focused LayoutAccessibility rerun passed14/15 and failed
another activity assertion because a generic heading query matched Activity and
the route heading. No unrelated test/application edits were made; fresh required
CI remains mandatory. GitHub automatic rebase produced an unsigned commit; it was
removed from the owned branch and replaced using the verified signed-file route.
