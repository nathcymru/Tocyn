# SLA priority queue — 13 September 2026

Progresses #137. The dashboard's shared persisted SLA sort now uses a complete authorized server snapshot, running unmet response/resolution deadlines, and priority/oldest creation/stable ID ties. Both Inbox and ticket list consume the same-as-of SLA values. Signed 30-second cursors restart explicitly when input changes or expires; restarting preserves the selected conversation. Numbered pages above one and offset pagination cannot override the cursor. Empty queues report zero total pages. Ordinary sorts remain available.

The backend preserves frozen and legacy live-policy semantics. A metadata stage bounds escaped UTF8 inputs before full snapshot transfer, including latest indexed snippets and complete pause histories. A shared optional deterministic evaluator meter propagates sticky exhaustion. Current credential, maintained scan and every snapshot ticket's visibility are checked before final operation journaling; route settlement follows response construction. Whole-input or work exhaustion returns unavailable, never a sampled or page-local substitute.

## Validation and limits

On the frozen source integrated with main df775ab748311f723ed3e68f776305352d8420a9, 873 ordinary server tests passed across 88 files. Server and specialist runtime types passed. Dashboard verification supplied by the coordinator: 369 tests across 52 files, types and build passed; this includes seven cursor-hook regressions and one Inbox integration. Dashboard has no supported standalone lint command; failed attempts to invoke one are not lint evidence. Server scoped ESLint passed before the disjoint main integration; routine CI remains unchanged.

Eight distinct native cases passed: global 33-ticket pagination and ties, full target/pause/frozen/legacy semantics, saved-filter changes and visibility revocation, whole-input overflow, legal expensive calendar/shared-meter exhaustion, bounded indexed latest snippet with unrelated older history, UTF8/escaped timestamp and pause bounds, and actual guarded authenticated HTTP including filters, empty results, pagination rejection and final settlement. The pause-negative fixture initially violated its existing ordering constraint; its end was made NULL, without changing product validation. A HTTP fixture initially omitted required guarded bootstrap; the existing bootstrap corrected that setup.

The 33-ticket four-page observation was 959.5 ms wall time and +5,252,400 bytes process heap delta. Metadata batches used 241–242 reads/4–5 writes; snapshot 705 reads/1 write; completion 107 reads/4 writes. The latest-snippet lookup used idx_articles_tenant_ticket_recent. These are synthetic observations, not peak heap or billed CPU measurements. The 4 MiB input and 12,000,000 work-unit limits are owner-approved local-candidate guards; no provider/runtime clearance is implied. Maximum-bound heap/time proof and integrated browser/accessibility acceptance remain release gates alongside the rest of #137/#73/#140. No preview promotion, release, provider activation or migration occurred.

## Routing and delivery

GPT retained consequential SQL, authorization, accounting, implementation and executed validation. A coordinator-supplied bounded Graphify packet was checked against source. One free cloud fixture proposal (639 input/1000 output tokens) was rejected completely because it was truncated and reversed a priority tie; no cases were accepted and no retry loop followed. These counts are not total ChatGPT usage or measured token savings. No Spark, paid fallback or Copilot request was used.

The combined 20-file application/test overlay was copied byte-for-byte into a separate integration checkout; the original working checkout was preserved. Live automatic-review rules were inspected and remain manual-only. Signing, exact CI/CodeQL and coordinator review remain required. Preserve existing Project progress, baseline, forecasts and actual dates: this partial SLA integration does not justify claiming all assignment/fairness/concurrency and release acceptance complete.
