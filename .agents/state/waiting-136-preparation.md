# #136 implementation handoff — 11 September 2026

Backend foundation started in `/private/tmp/tocyn-beta2-136` on `codex/136-support-state-foundation` from accepted main `0206645`. Root owns integration and GitHub. The live #136 authority confirms additive tenant-defined support-state definitions, legacy projection, waiting/next-action semantics, labels, audit and safe deactivation; #73 owns SLA policy. `0030_support_state_foundation.sql` now adds tenant-qualified definitions and ticket facts while preserving `tickets.status` and compatibility seeds. Repository/API/audit wiring and full integration tests remain unfinished; do not claim issue completion or UI delivery.

Preserve the legacy four-category `tickets.status` projection and existing payloads. Add tenant-qualified support-state definitions, distinct internal/public labels, explicit human waiting reason and optional next-action input. Seed compatibility definitions; decide exact additive reference/backfill strategy against the current migration tip when work starts. Labels never confer permission. Reject definition deactivation while active references exist unless a validated atomic remapping operation is explicitly supplied; never strand tickets.

Implementation surfaces: new additive migration after current0029; new scoped definition repository plus repositories/interfaces.ts and composition; ticket read/update paths in repositories/index.ts; dashboard/v1 schemas and tenant-ticket.service.ts; shared Ticket optional fields; conversation-audit repository/service/types and history visibility. Use existing tenant/session/group boundaries and transactional state/audit mutation pattern. Reverify import impact before editing.

Tests: migration chain/legacy rows/foreign keys, old API payloads, cross-tenant definition references, inactive states, state/waiting audit visibility, atomic deactivation/failure recovery, real local two-tenant D1 integration. Keep private waiting reason/next-action information out of public facts unless explicitly public by contract.

#73 owns configurable response/resolution pause policy, 24/7 UTC default calendar, clock ledger, breach behavior and customer-safe handler display; #136 provides deterministic server-observed transition facts. Root must reconcile pause inputs against accepted roadmap/ADR before implementation. Configurable policy and absent implementation do not automatically require fresh owner decisions. Questions about automatic reply transitions or public labels must first be checked against existing behavior/approved contracts; do not invent a new owner blocker from the preparation worker's question list. Full SLA/waiting/ownership acceptance remains mandatory before beta.2.

## Coordinator validation — 11 September 2026

The initial worker stopped at its execution limit. Root corrected a nonexistent global tenants-table reference, the invalid one-state-per-category uniqueness constraint, and seeding that skipped tenants without tickets. The real local Miniflare D1 migration test now passes: full prior migration chain, byte-preserved legacy ticket columns, colliding tenant/ticket IDs, empty-ticket tenant defaults, custom states sharing a lifecycle category, cross-tenant reference rejection, referenced-definition deletion rejection, bounded private facts, and foreign-key integrity. The test typecheck passes and is included in CI. This is a schema foundation only; no API/UI is enabled. Future tenant/default initialization, audited atomic transitions, required-field enforcement, deactivation/remapping, legacy-client synchronization and public/private projection remain before acceptance. Do not merge this draft as completed136. No owner approval missing.

## Backend implementation handoff — 11 September 2026

The server foundation is now implemented without changing existing ticket payloads or dashboard/portal UI. Migration `0030` creates future-tenant compatibility defaults on first ticket, retains the four legacy lifecycle values, synchronizes direct legacy `tickets.status` updates to their matching compatibility definition, and clears private waiting facts on those legacy updates. It adds private tenant-qualified support-state audit records and prevents compatibility defaults from being deactivated.

`SupportStateRepository` and `SupportStateService` provide tenant-scoped definition creation/update/listing, internal/public-label separation, required waiting/next-action validation, an optimistic `changed_at` compare-and-swap transition, deterministic #73 lifecycle/presence input, customer-safe label projection, and atomic deactivation with an explicit replacement. Explicit transitions use an in-transaction marker so the legacy-status trigger cannot overwrite a custom state; they write both support-state audit evidence and existing internal conversation state events. The service reuses the verified scope and group check before a staff transition. No route or UI was added, so existing public payloads remain byte-shape compatible; integration should expose only the customer-safe projection to customer paths.

Actual local evidence: full server typecheck; focused support-state script typecheck; real Miniflare migration/runtime test with two tenants, group denial, cross-tenant denial, stale-CAS/no-side-effect failure, required-remap failure, legacy status synchronization, deactivation/remap and foreign-key check; existing v1/dashboard/audit tests (31 tests). `npm audit --json` and `npm audit --omit=dev --json` both reported zero vulnerabilities in the installed lockfile. Remaining #136 acceptance is server-route/UI integration, final customer visibility review, and root's broader integration/revision checks; the issue and beta gate must remain open.

## Guarded route increment — 11 September 2026

Authenticated additive routes now expose bounded operator definition inventory and ticket-state reads/transitions, plus administrator-only definition create/update/deactivation. Definition writes reuse the existing `general` settings capability guard, revalidation and SQL write fence; compatibility definitions remain immutable. State listing is limited to 100 and no longer initializes defaults while reading. Deactivation rejects more than 100 active references rather than beginning an unbounded remap transaction.

Ticket-state transitions use a monotonic per-ticket revision for compare-and-swap, while `changed_at` remains a deterministic SLA input. They bind the JWT session version accepted by authentication, capture it after ticket and group reads, and carry that fence through every event/fact/ticket write in the D1 batch. They conditionally account for local-beta conversation capacity only when the CAS change applies. A customer route applies existing widget authentication and ownership checks, returning only lifecycle, public label and timestamp. It never includes internal labels, waiting reason or next action. Existing legacy ticket routes/payloads remain unchanged.

Actual route evidence uses the disposable two-tenant Miniflare fixture: unauthenticated denial, bounded list rejection, authenticated definition creation, stale-CAS conflict with no overwrite, cross-tenant ticket denial, customer public projection/private-fact exclusion, session-revocation denial, live agent group denial/regrant, and a real local-beta transition. The local-beta test proves an accepted transition charges once, stale CAS does not charge, and stopped intake returns the established beta denial without changing state or capacity. The migration/runtime test also proves membership/session revocation after a captured write fence prevents a D1 batch change, including a revocation between middleware credential acceptance and service execution; it freezes a repeated timestamp to prove revision CAS remains safe, and rejects a remap above 100 references. Combined verification: server typecheck; support-state migration/runtime and route tests (5 tests); existing v1/dashboard/customer/audit Vitest suite (49 tests). Remaining acceptance is UI integration and root’s final integration checks; do not close #136 or claim beta readiness.

Coordinator validation 11 September 2026: Node22.19.0 full server suite passes515 tests/58files after using the matching locally built native SQLite binding (same installed package/source version; no download or shared install). Backend increment includes monotonic revision CAS, trusted request-session fences, bounded mutation bodies and five real D1 migration/route tests. UI worker is active; this checkpoint is partial and does not close #136. Root retains acceptance and integration ownership.
