# #64 / #151 knowledge source admission correction

- Branch/worktree: `codex/64-knowledge-admission` at the clean #151 candidate base `b635e07`.
- Ownership: bounded knowledge source capture, manifest preparation, version cleanup and the active knowledge workflow/handler seams. Retention migration `0052` remains owned by `/root/budget64_ai_admission`; its existing article cleanup API is preserved.
- Decision: capture one immutable per-version R2 object and a durable preparation job in the authenticated request. Every later source-manifest batch reads one fixed R2 range and appends at most 100 chunks. Historical source/vector cleanup is represented by one tenant/document/source-kind target and claimed in batches; no request updates or deletes an unbounded version history.
- Limits: 10 MiB valid UTF-8 source; 512-byte chunks; worst legal greedy packing is 509 bytes (`127` four-byte scalars plus one ASCII scalar), so `KNOWLEDGE_INDEX_MAX_CHUNKS = 20601`; manifest and vector cleanup claims are 100 rows. Version keys are immutable (`<source-key>/versions/<version>`).
- Resource envelopes: source capture reserves indexed D1 headroom plus one R2 Class A write; preparation reserves one Class B range read and 100 indexed D1 inserts; cleanup reserves one indexed 100-row claim/complete batch. Native Miniflare D1 metadata for source begin/capture/100-row preparation/failure was `[5,3,303,5,3,6,3]` rows written per batch, so the preparation and cleanup envelope is 512 and source capture is 16. Final integration must remeasure against the combined migration/index inventory, including #0053's new ticket/article scan counters.
- Evidence so far: TypeScript check and focused Miniflare D1/R2 checks cover mixed full-size UTF-8, same-document replacement, immutable source keys, partial failure cleanup, and later-chunk retrieval. Full 10 MiB continuation and combined storage-background suite are run at handoff after the final integration edit.
- Remaining boundary: this does not add PDF extraction, public retrieval UI, or a full #151/#152 completion claim.

## Root integration checkpoint — 11 September2026

Root owns this candidate, refreshed on accepted mainfd8a679 (PR200). Worker50933087 passed693 units and19 native tests, but the standalone five-batch integration fixture was not covered by that claim and still invoked a legacy workflow. Root retained the legacy-rejection negative and replaced the positive with actual versioned source preparation/indexing using local D1/R2/BudgetCoordinator, stock allocation windows and synthetic1024-dimension embedding.

This uncovered a real transition defect: preparation kept next_chunk_index at chunk_count, allowing the workflow to skip provider indexing. Ready preparation now resets the indexing cursor to0; native assertions require next0 and deny premature completion. All five standalone integration batches now pass (log /private/tmp/tocyn-knowledge-integrated-d1.log). Final typecheck/unit/native suite is running under session59920; no final completion claim yet. This increment does not complete full64/151/152 or beta2.

Accepted PR200 evidence: signedfd8a679, testedtree313d9d0777ed54f5fcdf52412f49790b660789ed, CI34602046594/CodeQL34602045595, issue64 receipt5635048529. LiveWiki75163a1 synchronized to acceptedHome. Realtime20197f396a is under finalCI; retention0052, workspace0054, admin0055 worker-owned, list0053 awaits root integration/proof. No owner approval missing and no Copilot review requested.
