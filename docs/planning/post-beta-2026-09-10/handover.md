# Agent relaunch handover

## Release and authority

Owner-approved master package under #126. Beta.1 is published at `049ea82`; beta.2 is not yet created. Follow README→decisions→traceability→issue-map→forecast→owning issue. Sources/ZIPs and old beta state are historical inputs, not competing active instructions. Feature agents may resume only after #126 acceptance; review current main/CI and the issue before editing.

## Ready queue after alignment

- #127 workspace interaction contract: complete surface retain/migrate/retire inventory and durable-state/search/attention contracts before broad UI migration.
- #50 cost-policy/resource catalogue: include observability accounting and conservative admission/recovery contracts.
- #160 residency: source capability matrix and synthetic manifest validation; no remote provisioning.
- #79 granular authorization: existing tenancy/auth is foundation; governed capabilities are not already implemented.
- #12 required dependency-only checks and #14 format conventions may use spare capacity without disrupting the critical paths.

Use forecast.json for full dependencies and issue numbers. Foundations run independently; integration owner serializes overlapping changes. Issue numbers must never be guessed—read issue-map.

## Acceptance and review

Every worker returns actual changes, positive/negative/failure evidence, limits and remaining acceptance. No issue closes merely because a draft or implementation exists. Full #73 SLA and#137 routing are beta.2 blockers.#159/#160 feed #42 production readiness; real provider testing remains separately authorized. Preserve local synthetic mail and tenant/privacy boundaries.

Default zero Copilot requests. Automatic review trigger is removed. Standing owner approval permits PR-only review bypass after exact-revision required checks and signing; record bypass and findings disposition honestly. Do not purchase capacity or use API billing. Allocate lowest sufficient model/effort with native controls; escalate only difficult boundary work.

## Actual allocation and limitations

Discovery and document work used two native dedicated-Codex Luna/medium workers with bounded ownership; coordinator handled live GitHub, scheduling and integration. Work/Chat worker routes were unavailable; no cross-environment execution or separate allowance is claimed. Subscription ChatGPT authentication verified. Initial weekly Codex balance 29%, reset 15 September 02:24 BST; task-specific tokens/cost unknown. No reset credit used.

Historical worktrees with uncommitted source: `/private/tmp/tocyn-62-operator-workflows` (dashboard/operator work); `/private/tmp/tocyn-61-guarded-integration` (old state update). `/private/tmp/tocyn-21-mfa-correction` also contains generated local artifacts. Preserve them; compare narrowly before any later reuse. Do not reinstall dependencies in every worktree. Root checkout was clean before alignment.

Some GitHub milestone counters disagree with actual issue membership. Read issues and acceptance evidence, not counts. Current Wiki is a separate git repository; publish reviewed repository-backed sources and preserve historical-only pages. Any inaccessible surface must be named in the final receipt, never claimed synchronized.

## Integration receipt and relaunch condition

Alignment PR: [#163](https://github.com/nathcymru/Tocyn/pull/163). The final [#126 completion receipt](https://github.com/nathcymru/Tocyn/issues/126) records the exact signed merge, successful checks, Wiki revision/read-back and export checksum. This package is accepted for relaunch only when that receipt confirms completion. Read the receipt rather than interpreting pre-merge publication steps as a remaining feature blocker. Source-clause status stays pending until its owning feature issue has acceptance evidence.
