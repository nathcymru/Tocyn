<!-- tocyn-master-2026-09-10:PG-KB-02 -->
## Current authority

Approved master baseline #126. Direction is accepted; implementation remains incomplete until acceptance is evidenced. [Master package](https://github.com/nathcymru/Tocyn/blob/main/docs/planning/post-beta-2026-09-10/README.md). Alignment #126 is accepted and feature execution has resumed. The original packages are source proposals superseded by this reconciled record and its owner decisions.

**Proposed labels:** `enhancement`, `security`  
**Proposed effort:** 24 h baseline / 72 h planning  
**First-beta relevance:** Not a first-beta blocker; later capability/governance.

### Approved implementation scope

Formalise the logical per-tenant AI assistant contract so every retrieval is constrained by verified tenant + actor visibility and produces source/version provenance across operator and service-user AI surfaces.

### Scope boundary

No per-tenant model fine-tuning requirement; no cross-tenant shared retrieval cache; no third-party knowledge sync.

### Acceptance and demonstration


- [ ] Tenant and actor knowledge scope is derived server-side; client filters cannot grant access.

- [ ] Vectorize filters plus source-manifest ownership revalidation protect hydration.

- [ ] Operator-only/internal knowledge is not available to service-user AI.

- [ ] Suggestions/answers expose authorised source/page/version citations and staleness.

- [ ] Two tenants with intentionally similar documents prove zero cross-tenant retrieval.

- [ ] AI disabled/unavailable leaves deterministic KB/manual support usable.

- [ ] Bounded context and #50 cost controls apply.

- [ ] #76/#77 consume the contract without duplicating the RAG engine.



Architectural ordering is #151 → this issue → #76/#77. #76/#77 are consumers/integration targets, not blocking prerequisites. Preserve all retrieval scope/citation/visibility acceptance.

## Authoritative dependencies and forecast

Architectural prerequisites: #151, #79. Native blocked-by edges must match this list.

Remaining baseline effort: 24h; remaining planning allowance: 72h. Forecast: 2027-08-24–2027-09-10, W1. Existing baseline history is immutable; see [forecast ledger](https://github.com/nathcymru/Tocyn/blob/main/docs/planning/post-beta-2026-09-10/forecast.json). Two reference lanes,8h/day Monday–Saturday,2.5× implementation plus0.5× shared review/integration (4h/day). Dates are forecasts, not provider approvals or production commitments.

## Validation and execution policy

Preserve all scoped acceptance including tenant/actor isolation, negative/retry/failure cases, accessibility for new UI, resource ceilings and recovery evidence. No mocks substitute where real integration acceptance is required; unresolved provider access stays visible. All approved scope remains required even where delivery is sequenced later. Source sketches do not falsely assert deployed capability.

Default zero Copilot reviews. Use the standing owner-authorised PR-only approval bypass only after exact-revision required CI/security/signing and substantive findings pass; record bypass honestly. No production resources, provider activation, real customer messages, billing or additional release is authorized by this issue alone. Beta.1 has been formalised; beta.2 awaits #140 including full SLA/ownership/routing.


## Owner-approved compatibility correction — 10 September 2026

The owner explicitly approved a bounded correction to the existing article-marker contract under this issue. The dashboard must offer **Answer** and **SOP (internal procedure)** in agreement with the active tenant knowledge service and vector workflow. This corrects an existing integration defect; it does not claim the wider assistant/ingestion work is complete.

Acceptance for this correction:

- New and updated marker requests accept only `answer`, `sop` or `null`; unsupported values are rejected before mutation.
- UI labels, request types and regression tests agree with that contract. No silent `question` → `sop` mapping.
- Public/service-user retrieval remains Answer-only; SOP remains internal, with tenant/actor authorization unchanged.
- Existing Question records are retained for explicit compatibility/migration handling if required. Do not bulk convert or delete them as part of this correction.
- Demonstrate local tenant-scoped mark/unmark behavior, invalid-input non-mutation, internal/public retrieval boundaries and failure behavior using synthetic resources. Do not activate external AI/providers.
- Coordinate the UI consumer with #48 and record its implementation dependency. The existing #151 prerequisite continues to govern the full #152 delivery; this independently approved compatibility correction does not bypass or close the larger issue.
