# Assignment actions UI: partial #137 evidence

This slice builds on merged balanced-assignment PR #280 (`f9a18c729bcccacff26c3deea9012925f034e69b`). Progresses #137; remaining acceptance is not complete and the existing 15% estimate is preserved.

The selected ticket offers an explicit Balance assignment action beside Assigned To. The existing ordinary owner selector keeps normal capacity rules. Administrators can open an explicit capacity override dialog, select an operator and provide a nonempty reason within the backend's 512-byte UTF-8 bound. Current ownership is visible in the dialog; submission sends expected-owner CAS and the internal audit reason. Recipient eligibility and capacity are decided by the server; row data is not authorization or a sampled eligibility estimate.

Uncertain retries in the same identity/ticket component lifetime use the exact original key and body. No action submits automatically. Acknowledged and replayed outcomes refresh authoritative ownership and active queue queries. A failed confirmation read offers refresh only, never another mutation. No-capacity copy describes the prior attempt rather than asserting current ownership. Conflict retains the entered fields, reloads current ownership and requires a new explicit submission. Same-ticket field, support-state and reply submissions are blocked while assignment is unresolved; local draft editing is retained.

Navigation or identity changes abandon the local UI attempt, without claiming that server work was cancelled or did not commit. Layout-effect cleanup synchronously ends the old controller at committed navigation; the latest refresh callback is also committed in a layout effect, avoiding abandoned-render redirection. Every result also checks the current auth-store identity before notices, refetch or invalidation. There is no global pending store or reload-survival claim. Returning requires a successful fresh ticket fetch before new assignment controls are enabled.

Ticket detail query keys now include ticket, tenant, actor, role and authentication generation. The query function also fences its return against the captured identity. The outer detail and override panel keys use the same identity dimensions, so same-ID cached detail and entered override text do not render after identity changes. Existing prefix invalidations remain compatible. The exact-source audit found no exact detail set/get consumers requiring migration; the support-state subkey remains distinct. Existing AuthQueryBoundary creates a new QueryClient on generation/token change and cancels/clears the previous client; these detail guards additionally handle direct identity changes without generation increments. No broader hook/cache redesign is claimed.

Validation: 14 focused new hook/component cases pass; existing TicketDetailWorkflow cases passed in the affected run. Coverage includes exact-key retry, acknowledged refresh-only recovery, active unassigned-queue refetch, ticket/tenant/generation late-response discard, same-ID tenant and cached-admin-role isolation, conflict/new explicit CAS, required multibyte reason bound and administrator gating. Dashboard TypeScript and production build pass. An initial full dashboard run passed 360/361 with an unrelated ApiKeyRevokeDialog focus assertion failure; its untouched focused rerun passed 9/9. The full run after controller layout cleanup passed all 51 files / 361 tests. The subsequent callback-placement-only refinement passed the 14 focused cases and TypeScript; exact-revision CI remains mandatory.

The keyboard test uses the already independently reproduced JSDOM29 mixed-selector ordering and geometry accommodation from UsersCapacity, scoped to the test and restored afterward. It exercises standard TocynDialog opening, initial close focus, Tab/Shift+Tab containment, Escape and opener return in the simulator. This is not actual browser or spoken assistive-technology acceptance; coordinator browser validation remains required.

No unattended routing, presence lease, historic productivity scoring, SLA queue priority implementation, production deployment or full B2 acceptance is included. The existing backend performs tenant/current-role/group/capacity and atomic owner checks. The UI does not bypass those checks or silently add an override.
